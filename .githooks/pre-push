#!/usr/bin/env bash
# Hook local de pre-push: suíte completa antes de publicar. Substitui CI remota (sem GitHub Actions).
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

[ -f src/generated/content.json ] || pnpm -s content:build
pnpm -s test
pnpm -s build
