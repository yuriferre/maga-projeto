# E5 lote 1 — motor de avaliação + módulo M01: plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Entregar o motor de avaliação de módulo (conteúdo YAML próprio, itens + escrita + fala, elegibilidade, página) e o módulo M01 completo: quatro aulas novas (M01-01, M01-03, M01-04, M01-05), a avaliação do M01 e o padrão `br.actually` no catálogo de erros.

**Architecture:** A avaliação é conteúdo próprio (`content/modules/Mxx/assessment.yaml`, `ModuleAssessmentSchema`), com tentativas em `attempts` (`lesson_id = <módulo>`, `block = 'assessment'`, migração 2) e resultado em `assessments(kind='module')`. `server/assessment.ts` é puro (faltantes, elegibilidade, resultado); as rotas `/api/modules/:id/assessment/*` espelham as do placement. No cliente, `/modules/:id/assessment` reusa `ExerciseList` (feedback adiado), `Writing`, `Speaking` e um `AssessmentResult`. As aulas seguem o `LessonSchema` sem mudança.

**Tech Stack:** Node 25 (roda `.ts` direto), pnpm, TypeScript 7, Vite 8, React 19, react-router 8, Tailwind 4, Hono 4, `node:sqlite`, zod 4, yaml 2, vitest 5.

**Spec:** `docs/superpowers/specs/2026-09-11-e5-m01-design.md` (autoridade; leia antes). Origem: `docs/planejamento/02-trilha.md` (M01), `docs/planejamento/03-exemplo-aula-M01-02.md` (formato).

## Global Constraints

- **Branch:** `feat/e5-m01` (já criada). Sem `git push`, sem PR, sem `--no-verify`. Conventional Commits (hook `commit-msg`, assunto ≤ 72 chars); `pre-commit` valida conteúdo e roda typecheck.
- **Runtime:** Node ≥ 25 executa `.ts` direto. Imports relativos em `server/`, `shared/`, `scripts/` com extensão `.ts`. Sem `enum`, `namespace`, parameter properties. `noUncheckedIndexedAccess` ligado.
- **Dependências fechadas:** nada novo. `shared/` puro (sem DOM, sem `node:` exceto `content-loader.ts`); cliente importa tipos do servidor só com `import type`.
- **Sem LLM:** a E4 está fora do roteiro. Nada de `ANTHROPIC_API_KEY`, nada de chamada externa.
- **Texto:** UI e comentários em português com acentos; identificadores em inglês; conteúdo educacional em inglês (instruções, significados, explicações em português). Nunca placeholder. Caracteres tipográficos (`→ · — – ✓ ✗ ● ◼ ▶ …`) intactos; nunca substituir por ASCII.
- **YAML:** quote escalares com `#`, `: ` ou aspas iniciais; `''` para apóstrofo em string de aspas simples. Toda tag existe em `content/tags.yaml`. Mini-markdown só: parágrafos, `**negrito**`, `*itálico*` (sem aninhar), `` `código` ``, listas `- `, tabelas `|---|`. Depois de editar `content/`, `pnpm content:build`.
- **Regras de conteúdo (spec §4.1):** por aula, `vocabulary` 12–16, `examples` ≥ 6, `variations` ≥ 2, `brErrors` ≥ 8, `dialogue` 6–10 falas (M01-05: 12–16), `listening` 6 perguntas (M01-05: 8), `writing` 60–90 palavras (M01-05: 80–120) com 3–4 restrições regex, `speaking.modeA` ≥ 12 frases-alvo, `quiz` 8 itens com ≥ 4 tipos e gabarito balanceado (índice mais frequente ≤ 50 % dos itens de múltipla escolha), `srsCards` 10–12, `completion { quizMin: 0.75, writingMin: 3, speakingRequired: true }`. Ids de exercício `M01-0N-l1…`, `M01-0N-q1…`; cards com `front` único por aula e `meaning`/`front` únicos no bundle (o `crossValidate` já exige).
- **SQL parametrizado; todo POST com zod; timestamps só via `now()` injetável.**
- **Testes:** vitest, Node puro, conteúdo real, `openDb(":memory:")`, rotas via `app.request`. Nunca enfraqueça um teste existente. Suíte de partida: 180 testes.
- **Verificação por tarefa:** `pnpm content:build && pnpm typecheck && pnpm test` (cliente: `&& pnpm build`). UI verificada no Chrome na última tarefa.

---

## Estrutura de arquivos

```
shared/schema.ts             (T1) Block+assessment, ModuleFileSchema.pass, ModuleAssessmentSchema, ContentBundle.moduleAssessments
shared/content-loader.ts     (T1) lê assessment.yaml; crossValidate
content/modules/M01/module.yaml (T1) pass
server/db.ts                 (T2) migração 2
server/assessment.ts         (T2) novo: tipos, missingForAssessment, computeAssessmentResult, eligibility
server/repo.ts               (T3) moduleAssessmentSummary (overview)
server/app.ts                (T3) /api/attempts aceita assessment; sub-router /api/modules/:id/assessment; overview.modules
src/lib/api.ts, src/App.tsx  (T4)
src/pages/ModuleAssessment.tsx, src/components/assessment/AssessmentResult.tsx (T4) novos
src/pages/Module.tsx, src/pages/Levels.tsx, src/lib/useOverview.ts (T4)
content/br-errors.yaml, content/tags.yaml (T5) actually-currently, gram.time-expressions
content/modules/M01/lessons/M01-01.yaml (T6), M01-03.yaml (T7), M01-04.yaml (T8), M01-05.yaml (T9)
content/modules/M01/assessment.yaml (T10)
README.md, AGENTS.md         (T11)
tests/schema.test.ts, tests/content-loader.test.ts (T1); tests/db.test.ts, tests/assessment.test.ts (T2); tests/assessment-routes.test.ts (T3);
tests/br-detector.test.ts (T5); tests/lessons-m01.test.ts (T6–T9, criado em T6 e ampliado); tests/assessment-content.test.ts (T10)
```

Ordem: T1 → T2 → T3 → T4 (código) → T5 → T6 → T7 → T8 → T9 → T10 (conteúdo) → T11. T5–T9 só dependem de T1 (schema) e podem correr em paralelo com revisões de T2–T4; T10 depende de T3 (rota testada com o conteúdo real) e de T6–T9 (tags do módulo).

**Fixture de avaliação para T1–T4:** enquanto `content/modules/M01/assessment.yaml` não existe (T10), os testes de T1–T3 usam um `assessment.yaml` mínimo criado em T1 com 10 itens de placeholder? **Não** — conteúdo placeholder é proibido. Decisão: **T1 cria já a avaliação real do M01 em versão inicial com 10 itens** baseados apenas na M01-02 (única aula existente), e T10 amplia para 15 itens cobrindo as aulas novas. Assim nenhum teste depende de fixture falsa.

---

### Task 1: Schema, loader e `module.yaml` (avaliação como conteúdo)

**Files:**
- Modify: `shared/schema.ts`, `shared/content-loader.ts`, `content/modules/M01/module.yaml`
- Create: `content/modules/M01/assessment.yaml` (versão inicial, 10 itens da M01-02; T10 amplia)
- Test: `tests/schema.test.ts`, `tests/content-loader.test.ts`

**Interfaces:**
- Produces: `BlockSchema` aceita `"assessment"`; `ModuleFileSchema = { id, assessment: { description }, pass: { itemsMin, writingMin, speakingMin } }` (`pass` com defaults 0.75/3/3); `ModuleAssessmentSchema`/`ModuleAssessment = { id, title, intro, items: Exercise[] ≥ 10, writing: WritingSpec, speaking: SpeakingModeA }`; `ContentBundle.moduleAssessments: Record<string, ModuleAssessment>`; `loadContent` lê `content/modules/Mxx/assessment.yaml` quando existe; `crossValidate` cobre id = pasta, ids `Mxx-A..` únicos no bundle, tags, `minWords ≤ maxWords`, forma dos exercícios.

- [ ] **Step 1: Testes que falham**

Em `tests/schema.test.ts`, acrescente ao import `ModuleFileSchema, ModuleAssessmentSchema` e:

```ts
describe("ModuleFileSchema / ModuleAssessmentSchema", () => {
  it("module.yaml gets pass defaults 0.75 / 3 / 3", () => {
    const m = ModuleFileSchema.parse({ id: "M01", assessment: { description: "x" } });
    expect(m.pass).toEqual({ itemsMin: 0.75, writingMin: 3, speakingMin: 3 });
    expect(() => ModuleFileSchema.parse({ id: "M01", assessment: { description: "x" }, pass: { itemsMin: 1.5 } })).toThrow();
  });
  it("assessment needs at least 10 items", () => {
    const item = { id: "M01-A01", type: "multiple_choice", prompt: "p?", options: ["a", "b"], answer: 0, explanation: "e", tags: ["vocab.standup"] };
    const base = { id: "M01", title: "t", intro: "i", writing: { prompt: "w", rubric: ["r"], model: "m", minWords: 60, maxWords: 90 }, speaking: { prompt: "s", maxSeconds: 45, targetPhrases: ["no blockers"] } };
    expect(() => ModuleAssessmentSchema.parse({ ...base, items: [item] })).toThrow();
    const ten = Array.from({ length: 10 }, (_, i) => ({ ...item, id: `M01-A${String(i + 1).padStart(2, "0")}` }));
    expect(ModuleAssessmentSchema.parse({ ...base, items: ten }).items).toHaveLength(10);
  });
  it("BlockSchema accepts assessment", () => {
    expect(BlockSchema.parse("assessment")).toBe("assessment");
  });
});
```

Em `tests/content-loader.test.ts`, dentro de `describe("loadContent(content/)")`:

```ts
  it("loads the M01 module assessment with pass thresholds", () => {
    expect(bundle.modules["M01"]!.pass).toEqual({ itemsMin: 0.75, writingMin: 3, speakingMin: 3 });
    const a = bundle.moduleAssessments["M01"]!;
    expect(a.id).toBe("M01");
    expect(a.items.length).toBeGreaterThanOrEqual(10);
    expect(a.items.every((i) => i.id.startsWith("M01-A"))).toBe(true);
  });
```

e em `describe("crossValidate")`:

```ts
  it("reports an assessment whose id differs from its folder, a duplicated item id and an unknown tag", () => {
    const broken = structuredClone(bundle);
    const a = broken.moduleAssessments["M01"]!;
    a.items[1]!.id = a.items[0]!.id;
    a.items[2]!.tags = ["vocab.nope"];
    a.items[3]!.id = "M02-A04";
    const problems = crossValidate(broken);
    expect(problems.some((p) => p.includes("duplicad"))).toBe(true);
    expect(problems.some((p) => p.includes("vocab.nope"))).toBe(true);
    expect(problems.some((p) => p.includes("deveria começar com 'M01-A'"))).toBe(true);
  });
```

- [ ] **Step 2: Rode para ver falhar**

Run: `pnpm test tests/schema.test.ts tests/content-loader.test.ts`
Expected: FAIL — `ModuleAssessmentSchema` não existe; `pass`/`moduleAssessments` undefined.

- [ ] **Step 3: `shared/schema.ts`**

1. `BlockSchema`:

```ts
export const BlockSchema = z.enum(["warmup", "quiz", "listening", "writing", "speaking", "placement", "assessment"]);
```

2. Substitua `ModuleFileSchema` por:

```ts
// ---------- Módulo (metadados + regra de aprovação da avaliação) ----------
export const PassRuleSchema = z.object({
  /** Fração mínima de acertos nos itens objetivos. */
  itemsMin: z.number().min(0).max(1).default(0.75),
  /** Nota mínima da escrita (autoavaliação 1–5). */
  writingMin: z.number().min(1).max(5).default(3),
  /** Nota mínima da fala (modo A, 1–5). */
  speakingMin: z.number().min(1).max(5).default(3),
});
export type PassRule = z.infer<typeof PassRuleSchema>;
export const ModuleFileSchema = z.object({
  id: z.string().regex(/^M\d{2}$/),
  assessment: z.object({ description: z.string().min(1) }),
  pass: PassRuleSchema.default({ itemsMin: 0.75, writingMin: 3, speakingMin: 3 }),
});
export type ModuleFile = z.infer<typeof ModuleFileSchema>;

// ---------- Avaliação de módulo (conteúdo próprio: itens + escrita + fala) ----------
export const ModuleAssessmentSchema = z.object({
  id: z.string().regex(/^M\d{2}$/),
  title: z.string().min(1),
  intro: z.string().min(1),
  items: z.array(ExerciseSchema).min(10),
  writing: WritingSpecSchema,
  speaking: SpeakingModeASchema,
});
export type ModuleAssessment = z.infer<typeof ModuleAssessmentSchema>;
```

(`WritingSpecSchema`/`SpeakingModeASchema` já existem acima de `LessonSchema`; `ModuleFileSchema` precisa vir depois deles — mova o bloco de módulo para depois de `LessonSchema` se ainda não estiver.)

3. `ContentBundle`: acrescente `moduleAssessments: Record<string, ModuleAssessment>;`.

- [ ] **Step 4: `shared/content-loader.ts`**

Import: acrescente `ModuleAssessmentSchema` e `type ModuleAssessment`. Em `loadContent`, declare `const moduleAssessments: Record<string, ModuleAssessment> = {};` e, dentro do laço de módulos, logo depois do bloco de `module.yaml`:

```ts
      const assessmentFile = join(modulesDir, moduleId, "assessment.yaml");
      if (existsSync(assessmentFile)) {
        const a = readYaml(assessmentFile, ModuleAssessmentSchema, problems);
        if (a) {
          if (a.id !== moduleId) problems.push(`${assessmentFile}: id '${a.id}' não bate com a pasta '${moduleId}'`);
          if (!modules[moduleId]) problems.push(`${assessmentFile}: falta module.yaml com a regra de aprovação (pass)`);
          moduleAssessments[moduleId] = a;
        }
      }
```

Inclua `moduleAssessments` no objeto retornado. Em `crossValidate`, depois do bloco do placement:

```ts
  for (const a of Object.values(bundle.moduleAssessments)) {
    const where = `avaliação ${a.id}`;
    if (!roadmapModuleIds.has(a.id)) problems.push(`${where}: módulo não existe em levels.yaml`);
    checkTags(`${where}.writing`, a.writing.tags);
    if (a.writing.minWords > a.writing.maxWords) problems.push(`${where}: writing.minWords > maxWords`);
    for (const exercise of a.items) {
      if (seenExercise.has(exercise.id)) problems.push(`${where}: exercício duplicado '${exercise.id}'`);
      seenExercise.add(exercise.id);
      if (!exercise.id.startsWith(`${a.id}-A`)) problems.push(`${where}: exercício '${exercise.id}' deveria começar com '${a.id}-A'`);
      checkTags(`${where}.${exercise.id}`, exercise.tags);
      checkExerciseShape(exercise, problems);
    }
  }
```

- [ ] **Step 5: `content/modules/M01/module.yaml`**

```yaml
id: M01
assessment:
  description: "15 itens mistos das tags do módulo + escrita (update de standup, autoavaliada) + gravação de um update real (modo A). Aprovação: itens ≥ 75 %, escrita ≥ 3, fala ≥ 3."
pass:
  itemsMin: 0.75
  writingMin: 3
  speakingMin: 3
```

- [ ] **Step 6: `content/modules/M01/assessment.yaml` (versão inicial: 10 itens da M01-02; T10 amplia para 15)**

```yaml
# Avaliação do módulo M01. Versão inicial com 10 itens baseados na M01-02; a T10 do plano amplia para 15 cobrindo as cinco aulas.
id: M01
title: "Avaliação do módulo M01: daily standups"
intro: |
  Quinze itens objetivos, uma escrita e uma gravação. Sem consulta. Você não vê o resultado item a item até terminar.

  **Aprovação:** itens ≥ 75 %, escrita com autoavaliação ≥ 3, fala ≥ 3/5. Pode refazer; vale a última.
items:
  - { id: M01-A01, type: fill_blank, prompt: "I ___ (work) on the alerting rules since Tuesday.", accepted: ["have been working", "'ve been working"], explanation: "since + atividade que continua: present perfect continuous.", tags: [gram.since-for, br.since-present] }
  - { id: M01-A02, type: multiple_choice, prompt: "Which sentence sets a deadline?", options: ["I'll be on this until Friday.", "I'll have it done by Friday.", "I worked on it for Friday.", "I'm working on it since Friday."], answer: 1, explanation: "by = prazo final. until = duração; for/since marcam tempo decorrido.", tags: [gram.by-until, br.until-by] }
  - { id: M01-A03, type: error_correction, prompt: "I didn't merge the PR yet.", accepted: ["I haven't merged the PR yet.", "I haven't merged the PR yet"], explanation: "yet em negativa pede present perfect.", tags: [gram.still-yet-already, gram.present-perfect] }
  - { id: M01-A04, type: multiple_choice, prompt: "Which update is the clearest?", options: ["It's almost done, almost.", "The tests are taking longer than expected; I'll need another day.", "Everything is broken, I don't know.", "Sorry, sorry, I'm so slow."], answer: 1, explanation: "Diz o problema, o impacto e o novo prazo. As outras não dão informação.", tags: [topic.daily, vocab.standup] }
  - { id: M01-A05, type: fill_blank, prompt: "I'm still waiting ___ the security review.", accepted: ["on", "for"], explanation: "wait exige preposição: wait on / wait for.", tags: [br.waiting-no-prep] }
  - { id: M01-A06, type: error_correction, prompt: "I have a doubt about the rollout.", accepted: ["I have a question about the rollout.", "I have a question about the rollout", "I'm not sure about the rollout.", "I'm not sure about the rollout"], explanation: "doubt = desconfiança; pergunta é question.", tags: [br.doubt] }
  - { id: M01-A07, type: translate, prompt: "O job está dando erro de permissão.", accepted: ["The job is failing with a permission error.", "The job is failing with a permission error", "The job is throwing a permission error.", "The job is throwing a permission error", "The job fails with a permission error.", "The job fails with a permission error"], explanation: "\"Giving error\" é calque. Use fail with / throw / time out.", tags: [br.giving-error, vocab.errors] }
  - { id: M01-A08, type: reorder, prompt: "Coloque na ordem certa.", tokens: ["might", "tomorrow", "slip", "to", "this"], answer: "this might slip to tomorrow", explanation: "Aviso padrão de atraso: this might slip to + dia.", tags: [vocab.standup] }
  - { id: M01-A09, type: multiple_choice, prompt: "Yesterday I ___ the build stage.", options: ["fix", "have fixed", "fixed", "am fixing"], answer: 2, explanation: "yesterday fecha o momento: past simple.", tags: [gram.present-perfect] }
  - { id: M01-A10, type: multiple_choice, prompt: "\"Heads up: the deploy window may move.\" What is the speaker doing?", options: ["Complaining", "Giving an advance warning", "Asking for approval", "Reporting a bug"], answer: 1, explanation: "heads up = aviso antecipado.", tags: [vocab.standup, vocab.slack] }
writing:
  prompt: |
    **Cenário:** ontem você rotacionou as credenciais do banco de staging e atualizou os segredos no cluster. O deploy de `orders-api` falhou uma vez por causa de um segredo antigo em cache e você refez o rollout. Hoje pretende repetir o procedimento em produção, mas depende de uma janela aprovada pelo time de mudanças, pedida na segunda.

    **Tarefa:** escreva seu update de standup (60–90 palavras) para ser lido em voz alta.
  constraints:
    - { label: "1 present perfect", pattern: "\\b(I've|I have|it's been|it has been|has been|hasn't been|haven't|hasn't|we've|we have)\\b" }
    - { label: '"it turned out" ou "I've tried"', pattern: "\\b(it turned out|I've tried|I have tried)\\b" }
    - { label: '"waiting on/for" ou "blocked on"', pattern: "\\b(waiting (on|for)|blocked on)\\b" }
    - { label: '1 previsão com "by"', pattern: "\\bby (tomorrow|today|eod|end of|noon|lunch|friday|monday|tuesday|wednesday|thursday|the end|\\d)" }
  rubric:
    - Estrutura yesterday / today / blockers
    - Tempos verbais
    - Preposições e conectores
    - Naturalidade e registro
    - Concisão
  model: >-
    Yesterday I rotated the staging database credentials and updated the secrets in the cluster. The first
    orders-api deploy failed; it turned out an old secret was still cached, so I redid the rollout and it's been
    green since then. Today I'm going to repeat the procedure in production, but I'm still waiting on the change
    window I requested on Monday. If it gets approved this morning, I should have production done by EOD.
  minWords: 60
  maxWords: 90
  tags: [comp.writing, topic.daily, gram.present-perfect]
speaking:
  prompt: "Grave seu update de standup real de hoje em até 45 segundos: o que fez, o que vai fazer, bloqueios. Use as expressões do módulo."
  maxSeconds: 45
  targetPhrases:
    - I've been working on
    - I finished
    - I got it done
    - I'm still on
    - I'm blocked on
    - I'm waiting on
    - I'm waiting for
    - it turned out
    - I've tried
    - taking longer than expected
    - I should have it done by
    - might slip
    - heads up
    - no blockers
    - nothing blocking me
    - quick one
  checklist:
    - Usei past simple para ontem e present perfect para o estado atual?
    - Dei previsão com "by"?
    - Disse o que já tentei ou onde estou travado?
    - Fechei com "no blockers" ou um pedido claro?
```

- [ ] **Step 7: Verifique**

Run: `pnpm content:build && pnpm test && pnpm typecheck`
Expected: PASS (180 + 3 + 2 = 185). Se `crossValidate` acusar tag desconhecida, corrija o YAML (todas as tags acima existem em `tags.yaml`).

- [ ] **Step 8: Commit**

```bash
git add shared/schema.ts shared/content-loader.ts content/modules/M01/module.yaml content/modules/M01/assessment.yaml tests/schema.test.ts tests/content-loader.test.ts
git commit -m "feat(shared): schema e loader da avaliação de módulo; avaliação inicial do M01"
```

---

### Task 2: Migração 2 e `server/assessment.ts` (puro)

**Files:**
- Modify: `server/db.ts`
- Create: `server/assessment.ts`
- Test: `tests/db.test.ts`, `tests/assessment.test.ts` (novo)

**Interfaces:**
- Consumes: `ModuleAssessment`, `PassRule`, `Exercise` (T1); `AttemptRow`, `WritingRow`, `SpeakingRow`, `AssessmentRow`, `listProgress` (repo).
- Produces: `MIGRATIONS[2]`; em `server/assessment.ts`: `AssessmentKind = "module" | "level"`, `AssessmentSpec { kind; ref; items: Exercise[]; pass: PassRule }`, `AssessmentInputs { attempts: Map<string, AttemptRow>; writing?: WritingRow; speaking?: SpeakingRow }`, `AssessmentMissing { exercises: string[]; writing: boolean; speaking: boolean }`, `AssessmentResult { version: 1; kind; ref; itemCount; correct; itemsPct; writingScore: number | null; speakingScore: number | null; passed; pass: PassRule; weakTags: string[]; items: [{ id, correct, answer }]; finishedAt }`, `AssessmentRecord { id; ts; result }`, `missingForAssessment(spec, inputs)`, `computeAssessmentResult(spec, inputs, finishedAt)`, `parseAssessmentRecord(row)`, `moduleEligibility(content, progress, moduleId): { lessonsTotal; lessonsDone; missing: string[] }` (aulas com conteúdo do módulo não concluídas).

- [ ] **Step 1: Testes que falham**

`tests/db.test.ts`, novo bloco (o arquivo já importa `DatabaseSync`, `migrate`, `MIGRATIONS`):

```ts
describe("migration 2", () => {
  it("rebuilds attempts keeping rows and accepting block assessment", () => {
    const db = new DatabaseSync(":memory:");
    db.exec(MIGRATIONS[0]!);
    db.exec(MIGRATIONS[1]!);
    db.exec("create table schema_version (version integer not null); insert into schema_version (version) values (1)");
    db.prepare("insert into attempts (lesson_id, exercise_id, block, type, correct, answer, tags_json, ts) values ('placement','PL-r01','placement','multiple_choice',1,'x','[]','2026-09-01T10:00:00.000Z')").run();
    expect(migrate(db)).toBe(MIGRATIONS.length - 1);
    expect((db.prepare("select count(*) as n from attempts").get() as { n: number }).n).toBe(1);
    const insert = (block: string) => db.prepare("insert into attempts (lesson_id, exercise_id, block, type, correct, tags_json, ts) values ('M01','M01-A01',?,'multiple_choice',1,'[]','2026-09-02T10:00:00.000Z')").run(block);
    expect(() => insert("assessment")).not.toThrow();
    expect(() => insert("bogus")).toThrow();
    const indexes = (db.prepare("select name from sqlite_master where type='index' and tbl_name='attempts'").all() as { name: string }[]).map((r) => r.name);
    expect(indexes).toEqual(expect.arrayContaining(["idx_attempts_lesson_block", "idx_attempts_ts"]));
  });
});
```

`tests/assessment.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { loadContent } from "../shared/content-loader.ts";
import { computeAssessmentResult, missingForAssessment, moduleEligibility, parseAssessmentRecord, type AssessmentInputs, type AssessmentSpec } from "../server/assessment.ts";
import type { AttemptRow, WritingRow, SpeakingRow, LessonProgressRow } from "../server/repo.ts";

const content = loadContent("content");
const a = content.moduleAssessments["M01"]!;
const spec: AssessmentSpec = { kind: "module", ref: "M01", items: a.items, pass: content.modules["M01"]!.pass };
const T = "2026-09-11T12:00:00.000Z";

let seq = 0;
const attempt = (exerciseId: string, correct: boolean, tags: string[]): AttemptRow =>
  ({ id: ++seq, lesson_id: "M01", exercise_id: exerciseId, block: "assessment", type: "multiple_choice", correct: correct ? 1 : 0, answer: correct ? "ok" : "nope", score: null, tags_json: JSON.stringify(tags), ts: T });
const answers = (isCorrect: (id: string) => boolean) => new Map(a.items.map((q) => [q.id, attempt(q.id, isCorrect(q.id), q.tags)]));
const writing = (score: number | null): WritingRow => ({ id: 1, lesson_id: "M01", text: "t", feedback_json: "{}", score, ts: T });
const speaking = (score: number | null): SpeakingRow => ({ id: 1, lesson_id: "M01", mode: "A", transcript: "t", metrics_json: "{}", score, self_confidence: null, ts: T });
const inputs = (o: Partial<AssessmentInputs>): AssessmentInputs => ({ attempts: new Map(), writing: undefined, speaking: undefined, ...o });

describe("computeAssessmentResult", () => {
  it("passes when items ≥ 75 %, writing ≥ 3 and speaking ≥ 3", () => {
    const r = computeAssessmentResult(spec, inputs({ attempts: answers(() => true), writing: writing(3), speaking: speaking(3) }), T);
    expect(r).toMatchObject({ version: 1, kind: "module", ref: "M01", itemCount: a.items.length, correct: a.items.length, itemsPct: 1, writingScore: 3, speakingScore: 3, passed: true, finishedAt: T });
    expect(r.pass).toEqual(spec.pass);
    expect(r.items).toHaveLength(a.items.length);
    expect(r.weakTags).toEqual([]);
  });
  it.each([
    ["items below threshold", { items: 0.7, w: 5, s: 5 }],
    ["writing below threshold", { items: 1, w: 2, s: 5 }],
    ["speaking below threshold", { items: 1, w: 5, s: 2.5 }],
  ])("fails on %s", (_name, c) => {
    const n = Math.round(a.items.length * c.items);
    const r = computeAssessmentResult(spec, inputs({ attempts: answers((id) => a.items.findIndex((q) => q.id === id) < n), writing: writing(c.w), speaking: speaking(c.s) }), T);
    expect(r.passed).toBe(false);
  });
  it("uses the exact boundaries: 75 % passes, writing 3 passes, speaking 3 passes", () => {
    const twelve = Math.ceil(a.items.length * 0.75);
    const r = computeAssessmentResult(spec, inputs({ attempts: answers((id) => a.items.findIndex((q) => q.id === id) < twelve), writing: writing(3), speaking: speaking(3) }), T);
    expect(r.itemsPct).toBeGreaterThanOrEqual(0.75);
    expect(r.passed).toBe(true);
  });
  it("collects weak tags (error rate ≥ 50 %) and marks missing writing/speaking as null", () => {
    const r = computeAssessmentResult(spec, inputs({ attempts: answers((id) => id !== "M01-A05" && id !== "M01-A06") }), T);
    expect(r.writingScore).toBeNull();
    expect(r.speakingScore).toBeNull();
    expect(r.passed).toBe(false);
    expect(r.weakTags).toEqual(expect.arrayContaining(["br.waiting-no-prep", "br.doubt"]));
  });
});

describe("missingForAssessment", () => {
  it("lists unanswered items, missing writing score and missing speaking", () => {
    const att = answers(() => true);
    att.delete("M01-A03");
    expect(missingForAssessment(spec, inputs({ attempts: att, writing: writing(null) }))).toEqual({ exercises: ["M01-A03"], writing: true, speaking: true });
    expect(missingForAssessment(spec, inputs({ attempts: answers(() => true), writing: writing(4), speaking: speaking(4) }))).toEqual({ exercises: [], writing: false, speaking: false });
  });
});

describe("moduleEligibility", () => {
  const progress = (done: string[]): LessonProgressRow[] => done.map((id) => ({ lesson_id: id, status: "completed", score: 1, started_at: T, completed_at: T }));
  it("counts only lessons that have content and reports the missing ones", () => {
    const withContent = Object.keys(content.lessons).filter((id) => id.startsWith("M01-")).sort();
    expect(moduleEligibility(content, [], "M01")).toEqual({ lessonsTotal: withContent.length, lessonsDone: 0, missing: withContent });
    expect(moduleEligibility(content, progress(withContent), "M01")).toEqual({ lessonsTotal: withContent.length, lessonsDone: withContent.length, missing: [] });
  });
  it("is empty for a module without content", () => {
    expect(moduleEligibility(content, [], "M07")).toEqual({ lessonsTotal: 0, lessonsDone: 0, missing: [] });
  });
});

describe("parseAssessmentRecord", () => {
  it("parses score_json", () => {
    expect(parseAssessmentRecord({ id: 3, kind: "module", ref: "M01", score_json: JSON.stringify({ passed: true }), ts: T })).toMatchObject({ id: 3, ts: T, result: { passed: true } });
  });
});
```

- [ ] **Step 2: Rode para ver falhar**

Run: `pnpm test tests/db.test.ts tests/assessment.test.ts`
Expected: FAIL — `migrate` devolve 1; módulo `server/assessment.ts` inexistente.

- [ ] **Step 3: Migração em `server/db.ts`** — terceiro elemento de `MIGRATIONS`:

```ts
  // Migração 2: attempts aceita block='assessment' (avaliações de módulo/nível). SQLite não altera CHECK.
  `
  create table attempts_new (
    id integer primary key autoincrement,
    lesson_id text not null,
    exercise_id text not null,
    block text not null check (block in ('warmup','quiz','listening','writing','speaking','placement','assessment')),
    type text not null,
    correct integer not null check (correct in (0,1)),
    answer text,
    score real,
    tags_json text not null,
    ts text not null
  );
  insert into attempts_new (id, lesson_id, exercise_id, block, type, correct, answer, score, tags_json, ts)
    select id, lesson_id, exercise_id, block, type, correct, answer, score, tags_json, ts from attempts;
  drop table attempts;
  alter table attempts_new rename to attempts;
  create index idx_attempts_lesson_block on attempts(lesson_id, block);
  create index idx_attempts_ts on attempts(ts);
  `,
```

- [ ] **Step 4: `server/assessment.ts`**

```ts
import type { ContentBundle, Exercise, PassRule } from "../shared/schema.ts";
import type { AssessmentRow, AttemptRow, LessonProgressRow, SpeakingRow, WritingRow } from "./repo.ts";

export type AssessmentKind = "module" | "level";
export type AssessmentSpec = { kind: AssessmentKind; ref: string; items: Exercise[]; pass: PassRule };
export type AssessmentInputs = { attempts: Map<string, AttemptRow>; writing: WritingRow | undefined; speaking: SpeakingRow | undefined };
export type AssessmentMissing = { exercises: string[]; writing: boolean; speaking: boolean };
export type AssessmentResult = {
  version: 1;
  kind: AssessmentKind;
  ref: string;
  itemCount: number; correct: number; itemsPct: number;
  /** Autoavaliação 1–5 da escrita (sem LLM). */
  writingScore: number | null;
  /** Nota do modo A (1–5). */
  speakingScore: number | null;
  passed: boolean;
  pass: PassRule;
  /** Tags com erro ≥ 50 % entre os itens, mais erros primeiro. */
  weakTags: string[];
  items: Array<{ id: string; correct: boolean; answer: string | null }>;
  finishedAt: string;
};
export type AssessmentRecord = { id: number; ts: string; result: AssessmentResult };

/** Itens sem tentativa, escrita sem nota e fala ausente. Os três são obrigatórios para concluir. */
export function missingForAssessment(spec: AssessmentSpec, inputs: AssessmentInputs): AssessmentMissing {
  return {
    exercises: spec.items.filter((q) => !inputs.attempts.has(q.id)).map((q) => q.id),
    writing: inputs.writing === undefined || inputs.writing.score === null,
    speaking: inputs.speaking === undefined || inputs.speaking.score === null,
  };
}

function weakTagsOf(attempts: Iterable<AttemptRow>): string[] {
  const counts = new Map<string, { attempts: number; errors: number }>();
  for (const a of attempts) {
    for (const tag of JSON.parse(a.tags_json) as string[]) {
      const c = counts.get(tag) ?? { attempts: 0, errors: 0 };
      c.attempts++;
      if (a.correct === 0) c.errors++;
      counts.set(tag, c);
    }
  }
  return [...counts.entries()]
    .filter(([, c]) => c.errors / c.attempts >= 0.5)
    .sort((x, y) => y[1].errors - x[1].errors || x[0].localeCompare(y[0]))
    .map(([tag]) => tag);
}

/** Regra de aprovação (spec §3): itens ≥ itemsMin E escrita ≥ writingMin E fala ≥ speakingMin. */
export function computeAssessmentResult(spec: AssessmentSpec, inputs: AssessmentInputs, finishedAt: string): AssessmentResult {
  const items = spec.items.map((q) => {
    const a = inputs.attempts.get(q.id);
    return { id: q.id, correct: a?.correct === 1, answer: a?.answer ?? null };
  });
  const itemCount = items.length;
  const correct = items.filter((i) => i.correct).length;
  const itemsPct = itemCount === 0 ? 0 : correct / itemCount;
  const writingScore = inputs.writing?.score ?? null;
  const speakingScore = inputs.speaking?.score ?? null;
  const passed = itemsPct >= spec.pass.itemsMin && writingScore !== null && writingScore >= spec.pass.writingMin && speakingScore !== null && speakingScore >= spec.pass.speakingMin;
  return { version: 1, kind: spec.kind, ref: spec.ref, itemCount, correct, itemsPct, writingScore, speakingScore, passed, pass: spec.pass, weakTags: weakTagsOf(inputs.attempts.values()), items, finishedAt };
}

export function parseAssessmentRecord(row: AssessmentRow): AssessmentRecord {
  return { id: row.id, ts: row.ts, result: JSON.parse(row.score_json) as AssessmentResult };
}

/** Aulas do módulo que têm conteúdo e ainda não foram concluídas. Módulo sem conteúdo → tudo zero. */
export function moduleEligibility(content: Pick<ContentBundle, "levels" | "lessons">, progress: LessonProgressRow[], moduleId: string): { lessonsTotal: number; lessonsDone: number; missing: string[] } {
  const done = new Set(progress.filter((p) => p.status === "completed").map((p) => p.lesson_id));
  const refs = content.levels.flatMap((l) => l.modules).find((m) => m.id === moduleId)?.lessons ?? [];
  const withContent = refs.map((r) => r.id).filter((id) => id in content.lessons);
  const missing = withContent.filter((id) => !done.has(id));
  return { lessonsTotal: withContent.length, lessonsDone: withContent.length - missing.length, missing };
}
```

- [ ] **Step 5: Verifique**

Run: `pnpm test && pnpm typecheck`
Expected: PASS (185 + 1 + 9 = 195).

- [ ] **Step 6: Commit**

```bash
git add server/db.ts server/assessment.ts tests/db.test.ts tests/assessment.test.ts
git commit -m "feat(server): migração 2 e cálculo puro da avaliação de módulo"
```

---

### Task 3: Rotas da avaliação e `overview.modules`

**Files:**
- Modify: `server/app.ts`, `server/repo.ts`
- Test: `tests/assessment-routes.test.ts` (novo), `tests/app.test.ts` (1 asserção no overview)

**Interfaces:**
- Consumes: T1 (`ModuleAssessment`, `content.moduleAssessments`, `content.modules[id].pass`), T2 (`missingForAssessment`, `computeAssessmentResult`, `parseAssessmentRecord`, `moduleEligibility`, `AssessmentInputs`, `AssessmentRecord`), repo (`latestAttemptsSince`, `latestWritingSince`, `latestSpeakingSince`, `insertAssessment`, `latestAssessment`, `listProgress`, `insertWriting`, `insertSpeaking`), motores (`ruleBasedFeedback`, `computeSpeakingMetrics`).
- Produces: `POST /api/attempts` aceita `{ lessonId: "M01", block: "assessment", exerciseId: "M01-A01" }`; sub-router `/api/modules/:id/assessment` com `GET /state` → `{ eligible: { lessonsTotal, lessonsDone, missing }, latest: AssessmentRecord | null, run: { answered: string[], writing: WritingRow | null, speaking: SpeakingRow | null } }`, `POST /writing` (`WritingBody`) → `{ id, feedback }`, `POST /speaking` (`SpeakingBody`) → `{ id, metrics }`, `POST /finish` → 409 `{ error: "aulas pendentes", missing }` | 409 `{ error: "avaliação incompleta", missing }` | `{ assessment: AssessmentRecord }`; 404 `{ error: "módulo sem avaliação" }` para `:id` sem `assessment.yaml`; `GET /api/progress/overview` → `{ lessons, modules: Record<string, { passed: boolean; latest: AssessmentRecord | null }> }` (repo: `latestModuleAssessments(db): AssessmentRow[]`, uma linha por `ref`, a mais recente).

- [ ] **Step 1: Testes que falham** — `tests/assessment-routes.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import type { Hono } from "hono";
import { createApp } from "../server/app.ts";
import { openDb, type Db } from "../server/db.ts";
import { loadContent } from "../shared/content-loader.ts";
import { listAssessments } from "../server/repo.ts";

const content = loadContent("content");
const a = content.moduleAssessments["M01"]!;
const m01Lessons = Object.keys(content.lessons).filter((id) => id.startsWith("M01-")).sort();
let app: Hono;
let db: Db;
let clock = 0;
const now = () => new Date(Date.UTC(2026, 8, 11, 10, 0, clock++)).toISOString();

beforeEach(() => {
  clock = 0;
  db = openDb(":memory:");
  app = createApp({ db, content, now });
});
const json = (method: string, path: string, body?: unknown) =>
  app.request(path, { method, headers: { "content-type": "application/json" }, body: body === undefined ? undefined : JSON.stringify(body) });
const state = async () => (await app.request("/api/modules/M01/assessment/state")).json();

/** Conclui uma aula pelas rotas: quiz 100 %, escrita nota 4, uma fala. */
async function completeLesson(id: string) {
  const lesson = content.lessons[id]!;
  await json("POST", `/api/lessons/${id}/start`);
  for (const q of lesson.quiz) await json("POST", "/api/attempts", { lessonId: id, exerciseId: q.id, block: "quiz", type: q.type, correct: true, answer: "x", tags: q.tags });
  await json("POST", `/api/lessons/${id}/writing`, { text: lesson.writing.model, selfScore: 4 });
  await json("POST", `/api/lessons/${id}/speaking`, { mode: "A", transcript: lesson.speaking.modeA.targetPhrases.slice(0, 5).join(". ") + ". No blockers.", durationSec: 20 });
  const done = await (await json("POST", `/api/lessons/${id}/complete`)).json();
  expect(done.progress.status, id).toBe("completed");
}
async function answerAll(isCorrect: (id: string) => boolean) {
  for (const q of a.items) {
    const res = await json("POST", "/api/attempts", { lessonId: "M01", exerciseId: q.id, block: "assessment", type: q.type, correct: isCorrect(q.id), answer: "x", tags: q.tags });
    expect(res.status).toBe(200);
  }
}
const goodSpeech = a.speaking.targetPhrases.slice(0, 5).join(". ") + ". No blockers.";

describe("attempts for assessments", () => {
  it("accepts an assessment attempt and rejects wrong block / unknown item / unknown module", async () => {
    expect((await json("POST", "/api/attempts", { lessonId: "M01", exerciseId: "M01-A01", block: "assessment", type: "fill_blank", correct: true, tags: ["gram.since-for"] })).status).toBe(200);
    expect((await json("POST", "/api/attempts", { lessonId: "M01", exerciseId: "M01-A01", block: "quiz", type: "fill_blank", correct: true, tags: [] })).status).toBe(400);
    expect((await json("POST", "/api/attempts", { lessonId: "M01", exerciseId: "M01-Z99", block: "assessment", type: "fill_blank", correct: true, tags: [] })).status).toBe(400);
    expect((await json("POST", "/api/attempts", { lessonId: "M07", exerciseId: "M07-A01", block: "assessment", type: "fill_blank", correct: true, tags: [] })).status).toBe(404);
  });
});

describe("/api/modules/:id/assessment", () => {
  it("404 for a module without assessment.yaml", async () => {
    expect((await app.request("/api/modules/M07/assessment/state")).status).toBe(404);
  });
  it("state reports eligibility and an empty run", async () => {
    const s = await state();
    expect(s.eligible).toEqual({ lessonsTotal: m01Lessons.length, lessonsDone: 0, missing: m01Lessons });
    expect(s).toMatchObject({ latest: null, run: { answered: [], writing: null, speaking: null } });
  });
  it("finish refuses with pending lessons", async () => {
    await answerAll(() => true);
    const res = await json("POST", "/api/modules/M01/assessment/finish");
    expect(res.status).toBe(409);
    expect((await res.json()).error).toBe("aulas pendentes");
  });
  it("full flow: lessons → items → writing → speaking → finish passes; overview and timeline reflect it", async () => {
    for (const id of m01Lessons) await completeLesson(id);
    expect((await state()).eligible.missing).toEqual([]);
    await answerAll(() => true);
    let res = await json("POST", "/api/modules/M01/assessment/finish");
    expect(res.status).toBe(409);
    expect((await res.json()).missing).toEqual({ exercises: [], writing: true, speaking: true });
    expect((await json("POST", "/api/modules/M01/assessment/writing", { text: a.writing.model, selfScore: 4 })).status).toBe(200);
    const sp = await (await json("POST", "/api/modules/M01/assessment/speaking", { mode: "A", transcript: goodSpeech, durationSec: 25 })).json();
    expect(sp.metrics.score).toBeGreaterThanOrEqual(3);
    res = await json("POST", "/api/modules/M01/assessment/finish");
    expect(res.status).toBe(200);
    const { assessment } = await res.json();
    expect(assessment.result).toMatchObject({ kind: "module", ref: "M01", passed: true, itemsPct: 1, writingScore: 4 });
    const s = await state();
    expect(s.latest.id).toBe(assessment.id);
    expect(s.run).toEqual({ answered: [], writing: null, speaking: null });
    const overview = await (await app.request("/api/progress/overview")).json();
    expect(overview.modules.M01).toMatchObject({ passed: true, latest: { id: assessment.id } });
    expect(listAssessments(db).filter((r) => r.kind === "module")).toHaveLength(1);
  });
  it("fails on a low speaking score and a retake can pass", async () => {
    for (const id of m01Lessons) await completeLesson(id);
    await answerAll(() => true);
    await json("POST", "/api/modules/M01/assessment/writing", { text: a.writing.model, selfScore: 4 });
    await json("POST", "/api/modules/M01/assessment/speaking", { mode: "A", transcript: "I have a doubt, it's giving error and I'm waiting the review", durationSec: 70 });
    const first = (await (await json("POST", "/api/modules/M01/assessment/finish")).json()).assessment;
    expect(first.result.passed).toBe(false);
    expect(first.result.speakingScore).toBeLessThan(3);
    await answerAll(() => true);
    await json("POST", "/api/modules/M01/assessment/writing", { text: a.writing.model, selfScore: 4 });
    await json("POST", "/api/modules/M01/assessment/speaking", { mode: "A", transcript: goodSpeech, durationSec: 25 });
    const second = (await (await json("POST", "/api/modules/M01/assessment/finish")).json()).assessment;
    expect(second.result.passed).toBe(true);
    expect((await state()).latest.id).toBe(second.id);
    expect((await (await app.request("/api/progress/overview")).json()).modules.M01.passed).toBe(true);
  });
  it("400 on invalid bodies", async () => {
    expect((await json("POST", "/api/modules/M01/assessment/writing", { text: " " })).status).toBe(400);
    expect((await json("POST", "/api/modules/M01/assessment/speaking", { mode: "B", transcript: "x", durationSec: 1 })).status).toBe(400);
  });
});
```

Em `tests/app.test.ts`, no teste "overview and tag stats reflect attempts", acrescente `expect(overview.modules).toEqual({});` logo após `expect(overview.lessons).toHaveLength(1);`.

- [ ] **Step 2: Rode para ver falhar**

Run: `pnpm test tests/assessment-routes.test.ts tests/app.test.ts`
Expected: FAIL — rotas 404; `overview.modules` undefined.

- [ ] **Step 3: `server/repo.ts`** — depois de `listAssessments`:

```ts
/** Última avaliação de cada módulo (kind = 'module'), uma linha por ref. */
export function latestModuleAssessments(db: Db): AssessmentRow[] {
  return db
    .prepare(
      `select a.* from assessments a
       where a.kind = 'module' and a.id = (select max(b.id) from assessments b where b.kind = 'module' and b.ref = a.ref)
       order by a.ref`,
    )
    .all() as AssessmentRow[];
}
```

- [ ] **Step 4: `server/app.ts`**

Imports: `latestModuleAssessments` (repo); `import { computeAssessmentResult, missingForAssessment, moduleEligibility, parseAssessmentRecord, type AssessmentInputs } from "./assessment.ts";`.

Overview:

```ts
  app.get("/api/progress/overview", (c) => {
    const modules = Object.fromEntries(latestModuleAssessments(db).map((r) => {
      const rec = parseAssessmentRecord(r);
      return [r.ref, { passed: rec.result.passed, latest: rec }];
    }));
    return c.json({ lessons: listProgress(db), modules });
  });
```

`POST /api/attempts`: dentro de `createApp`, ao lado de `placementIds`, `const assessmentIds = new Map(Object.entries(content.moduleAssessments).map(([id, a]) => [id, new Set(a.items.map((q) => q.id))]));`. Na rota, antes do `else` da aula:

```ts
    } else if (block === "assessment") {
      const ids = assessmentIds.get(lessonId);
      if (!ids) return c.json({ error: "módulo sem avaliação" }, 404);
      if (!ids.has(exerciseId)) return c.json({ error: "exercício não pertence à avaliação" }, 400);
    } else {
```

(O ramo `placement` continua primeiro; o ramo de aula continua rejeitando `block === "placement"` e agora também fica implícito que `assessment` nunca chega nele.)

Sub-router, antes de `app.route("/api/placement", placement);`:

```ts
  // ---------- avaliação de módulo ----------
  const moduleAssessment = new Hono();

  moduleAssessment.use("/:id/*", async (c, next) => {
    if (!content.moduleAssessments[c.req.param("id") ?? ""]) return c.json({ error: "módulo sem avaliação" }, 404);
    await next();
  });

  /** Rodada atual do módulo: tudo gravado com lesson_id = id depois da última avaliação dele. */
  const assessmentRun = (id: string): AssessmentInputs => {
    const since = latestAssessment(db, "module", id)?.ts ?? "";
    return {
      attempts: latestAttemptsSince(db, id, "assessment", since),
      writing: latestWritingSince(db, id, since),
      speaking: latestSpeakingSince(db, id, since),
    };
  };

  moduleAssessment.get("/:id/assessment/state", (c) => {
    const id = c.req.param("id");
    const latestRow = latestAssessment(db, "module", id);
    const run = assessmentRun(id);
    return c.json({
      eligible: moduleEligibility(content, listProgress(db), id),
      latest: latestRow ? parseAssessmentRecord(latestRow) : null,
      run: { answered: [...run.attempts.keys()], writing: run.writing ?? null, speaking: run.speaking ?? null },
    });
  });

  moduleAssessment.post("/:id/assessment/writing", async (c) => {
    const id = c.req.param("id");
    const parsed = WritingBody.safeParse(await c.req.json().catch(() => null));
    if (!parsed.success) return c.json({ error: "corpo inválido", issues: parsed.error.issues }, 400);
    const feedback = ruleBasedFeedback(parsed.data.text, content.moduleAssessments[id]!.writing, content.brErrors, parsed.data.selfScore);
    const rowId = insertWriting(db, { lessonId: id, text: parsed.data.text, feedback, score: feedback.score }, now());
    return c.json({ id: rowId, feedback });
  });

  moduleAssessment.post("/:id/assessment/speaking", async (c) => {
    const id = c.req.param("id");
    const parsed = SpeakingBody.safeParse(await c.req.json().catch(() => null));
    if (!parsed.success) return c.json({ error: "corpo inválido", issues: parsed.error.issues }, 400);
    const metrics = computeSpeakingMetrics(parsed.data.transcript, parsed.data.durationSec, content.moduleAssessments[id]!.speaking, content.brErrors);
    const rowId = insertSpeaking(db, { lessonId: id, mode: "A", transcript: parsed.data.transcript, metrics, score: metrics.score, selfConfidence: parsed.data.selfConfidence ?? null }, now());
    return c.json({ id: rowId, metrics });
  });

  moduleAssessment.post("/:id/assessment/finish", (c) => {
    const id = c.req.param("id");
    const eligible = moduleEligibility(content, listProgress(db), id);
    if (eligible.missing.length > 0) return c.json({ error: "aulas pendentes", missing: eligible.missing }, 409);
    const spec = { kind: "module" as const, ref: id, items: content.moduleAssessments[id]!.items, pass: content.modules[id]!.pass };
    const inputs = assessmentRun(id);
    const missing = missingForAssessment(spec, inputs);
    if (missing.exercises.length > 0 || missing.writing || missing.speaking) return c.json({ error: "avaliação incompleta", missing }, 409);
    const ts = now();
    const result = computeAssessmentResult(spec, inputs, ts);
    const rowId = insertAssessment(db, { kind: "module", ref: id, score: result }, ts);
    return c.json({ assessment: { id: rowId, ts, result } });
  });

  app.route("/api/modules", moduleAssessment);
```

Na rota `finish`, use `content.modules[id]?.pass ?? { itemsMin: 0.75, writingMin: 3, speakingMin: 3 }` em vez de `content.modules[id]!.pass` (um módulo com `assessment.yaml` sem `module.yaml` é acusado pelo loader, mas a rota não deve estourar).

- [ ] **Step 5: Verifique**

Run: `pnpm test && pnpm typecheck`
Expected: PASS (195 + 7 = 202). O teste "full flow" completa 1 aula hoje (só a M01-02 tem conteúdo); quando T6–T9 entrarem, ele passa a completar 5 sem mudar.

- [ ] **Step 6: Commit**

```bash
git add server/app.ts server/repo.ts tests/assessment-routes.test.ts tests/app.test.ts shared/content-loader.ts
git commit -m "feat(server): rotas da avaliação de módulo e resumo no overview"
```

---

### Task 4: Cliente — página da avaliação, card no módulo, badge na trilha

**Files:**
- Modify: `src/lib/api.ts`, `src/App.tsx`, `src/lib/useOverview.ts`, `src/pages/Module.tsx`, `src/pages/Levels.tsx`
- Create: `src/pages/ModuleAssessment.tsx`, `src/components/assessment/AssessmentResult.tsx`

**Interfaces:**
- Consumes: rotas da T3; `ExerciseList` (`feedback="deferred"`, `initialAnswered`), `Writing`, `Speaking`, `Stepper readOnly`, `Markdown`, `Button`, `Card`, `Badge`; `content.moduleAssessments`, `findModule`, `tagLabel`; tipos via `import type`: `AssessmentRecord`, `AssessmentMissing` (`server/assessment.ts`), `ModuleAssessment` (`shared/schema.ts`).
- Produces: `api.moduleAssessmentState(id)`, `api.submitModuleWriting(id, body)`, `api.submitModuleSpeaking(id, body)`, `api.finishModuleAssessment(id)`; `api.overview()` tipado com `modules`; `useOverview()` devolve também `modules`; rota `/modules/:id/assessment`.
- Sem teste automatizado (UI). Verificação: `pnpm typecheck && pnpm build && pnpm test`.

- [ ] **Step 1: `src/lib/api.ts`**

Imports: `import type { AssessmentRecord, AssessmentMissing } from "../../server/assessment.ts";`. Tipos:

```ts
export type ModuleSummary = { passed: boolean; latest: AssessmentRecord | null };
export type ModuleAssessmentState = {
  eligible: { lessonsTotal: number; lessonsDone: number; missing: string[] };
  latest: AssessmentRecord | null;
  run: { answered: string[]; writing: WritingRow | null; speaking: SpeakingRow | null };
};
```

Acrescente `AssessmentRecord, AssessmentMissing, ModuleSummary` ao `export type { ... }`. Em `api`: troque `overview` por `overview: () => request<{ lessons: LessonProgressRow[]; modules: Record<string, ModuleSummary> }>("/api/progress/overview"),` e acrescente:

```ts
  // avaliação de módulo
  moduleAssessmentState: (id: string) => request<ModuleAssessmentState>(`/api/modules/${id}/assessment/state`),
  submitModuleWriting: (id: string, body: { text: string; selfScore?: number }) => post<{ id: number; feedback: WritingFeedback }>(`/api/modules/${id}/assessment/writing`, body),
  submitModuleSpeaking: (id: string, body: { mode: "A"; transcript: string; durationSec: number; selfConfidence?: number }) =>
    post<{ id: number; metrics: SpeakingMetrics }>(`/api/modules/${id}/assessment/speaking`, body),
  finishModuleAssessment: (id: string) => post<{ assessment: AssessmentRecord }>(`/api/modules/${id}/assessment/finish`),
```

- [ ] **Step 2: `src/lib/useOverview.ts`** — guarde também os módulos:

```ts
import { useCallback, useEffect, useState } from "react";
import { api, type ModuleSummary } from "./api.ts";
import type { LessonProgressRow } from "../../server/repo.ts";

export function useOverview() {
  const [byLesson, setByLesson] = useState<Map<string, LessonProgressRow>>(new Map());
  const [modules, setModules] = useState<Record<string, ModuleSummary>>({});
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const reload = useCallback(() => {
    setLoading(true);
    api.overview()
      .then(({ lessons, modules }) => { setByLesson(new Map(lessons.map((l) => [l.lesson_id, l]))); setModules(modules); setError(null); })
      .catch((err: Error) => setError(err.message))
      .finally(() => setLoading(false));
  }, []);

  useEffect(() => { reload(); }, [reload]);
  return { byLesson, modules, loading, error, reload };
}
```

- [ ] **Step 3: `src/components/assessment/AssessmentResult.tsx`**

```tsx
import { Link } from "react-router";
import type { Exercise } from "../../../shared/schema.ts";
import type { AssessmentRecord } from "../../lib/api.ts";
import { tagLabel } from "../../lib/content.ts";
import { Badge } from "../ui/Badge.tsx";
import { Button } from "../ui/Button.tsx";
import { Card } from "../ui/Card.tsx";

const pct = (n: number) => `${Math.round(n * 100)}%`;
const score = (n: number | null) => (n === null ? "—" : `${n}/5`);

function expectedOf(ex: Exercise): string {
  if (ex.type === "multiple_choice") return ex.options[ex.answer] ?? "";
  if ("accepted" in ex) return ex.accepted[0] ?? "";
  if ("answer" in ex) return String(ex.answer);
  return "";
}
function shownAnswer(ex: Exercise, answer: string | null): string {
  if (answer === null) return "—";
  if (ex.type === "multiple_choice" && /^\d+$/.test(answer)) return ex.options[Number(answer)] ?? answer;
  return answer;
}

export function AssessmentResult({ record, items, title, backTo, onRetake }: { record: AssessmentRecord; items: Exercise[]; title: string; backTo: { to: string; label: string }; onRetake(): void }) {
  const r = record.result;
  const byId = new Map(items.map((q) => [q.id, q]));
  const date = new Date(record.ts).toLocaleString("pt-BR");
  const rule = `itens ≥ ${pct(r.pass.itemsMin)}, escrita ≥ ${r.pass.writingMin}, fala ≥ ${r.pass.speakingMin}`;
  return (
    <div className="space-y-6">
      <Card>
        <p className="text-sm text-slate-500">{title} · {date}</p>
        <h2 className={`mt-1 text-2xl font-semibold ${r.passed ? "text-emerald-800" : "text-rose-800"}`}>{r.passed ? "Aprovado" : "Ainda não"}</h2>
        <p className="mt-2 text-slate-700">Itens {r.correct}/{r.itemCount} ({pct(r.itemsPct)}) · escrita {score(r.writingScore)} · fala {score(r.speakingScore)}. Regra: {rule}.</p>
        <p className="mt-2 text-xs text-slate-500">A nota de escrita é a sua autoavaliação guiada pela rubrica; a de fala vem das expressões-alvo, erros do catálogo, tempo e ritmo.</p>
        <div className="mt-4 flex flex-wrap gap-3">
          <Link to={backTo.to} className="rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white hover:bg-indigo-700">{backTo.label}</Link>
          <Button variant="ghost" onClick={onRetake}>Refazer</Button>
        </div>
      </Card>
      <Card>
        <h3 className="font-medium">Tags fracas ({r.weakTags.length})</h3>
        <div className="mt-2 flex flex-wrap gap-1">{r.weakTags.length === 0 ? <span className="text-sm text-slate-500">nenhuma</span> : r.weakTags.map((t) => <Badge key={t} tone="red">{tagLabel(t)}</Badge>)}</div>
      </Card>
      <Card>
        <h3 className="font-medium">Revisão item a item</h3>
        <ol className="mt-3 space-y-3 text-sm">
          {r.items.map((item, i) => {
            const ex = byId.get(item.id);
            if (!ex) return null;
            return (
              <li key={item.id} className={`rounded-md border p-3 ${item.correct ? "border-emerald-200 bg-emerald-50" : "border-rose-200 bg-rose-50"}`}>
                <div className="text-xs text-slate-500">{i + 1}</div>
                <div className="font-medium">{ex.prompt}</div>
                <div>{item.correct ? "✓" : "✗"} Sua resposta: {shownAnswer(ex, item.answer)}{!item.correct && <> · Esperado: <span className="font-medium">{expectedOf(ex)}</span></>}</div>
                <div className="mt-1 text-slate-600">{ex.explanation}</div>
              </li>
            );
          })}
        </ol>
      </Card>
    </div>
  );
}
```

- [ ] **Step 4: `src/pages/ModuleAssessment.tsx`**

```tsx
import { useEffect, useState } from "react";
import { Link, useParams } from "react-router";
import type { SpeakingMetrics } from "../../server/speaking-metrics.ts";
import { api, ApiError, type AssessmentMissing, type AssessmentRecord, type ModuleAssessmentState } from "../lib/api.ts";
import { content, findModule } from "../lib/content.ts";
import { Button } from "../components/ui/Button.tsx";
import { Card } from "../components/ui/Card.tsx";
import { Markdown } from "../components/ui/Markdown.tsx";
import { Stepper } from "../components/lesson/Stepper.tsx";
import { ExerciseList } from "../components/exercises/ExerciseList.tsx";
import { Writing } from "../components/lesson/Writing.tsx";
import { Speaking } from "../components/lesson/Speaking.tsx";
import { AssessmentResult } from "../components/assessment/AssessmentResult.tsx";

type Stage = "loading" | "locked" | "intro" | "items" | "writing" | "speaking" | "result";
const STEPS = [{ key: "items", label: "Itens" }, { key: "writing", label: "Escrita" }, { key: "speaking", label: "Fala" }, { key: "result", label: "Resultado" }];

export function ModuleAssessment() {
  const { id = "" } = useParams();
  const assessment = content.moduleAssessments[id];
  const found = findModule(id);
  const [stage, setStage] = useState<Stage>("loading");
  const [state, setState] = useState<ModuleAssessmentState | null>(null);
  const [record, setRecord] = useState<AssessmentRecord | null>(null);
  const [writingScore, setWritingScore] = useState<number | null>(null);
  const [spoken, setSpoken] = useState<SpeakingMetrics | null>(null);
  const [missing, setMissing] = useState<AssessmentMissing | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);

  /** Carrega o estado e decide o estágio: resultado, retomada ou intro. */
  const load = (afterRetake = false) => {
    setError(null); setMissing(null);
    api.moduleAssessmentState(id).then((s) => {
      setState(s);
      const hasRun = s.run.answered.length > 0 || s.run.writing !== null || s.run.speaking !== null;
      if (s.eligible.missing.length > 0) { setStage("locked"); return; }
      if (s.latest && !hasRun && !afterRetake) { setRecord(s.latest); setStage("result"); return; }
      setWritingScore(s.run.writing?.score ?? null);
      if (s.run.speaking) setSpoken(JSON.parse(s.run.speaking.metrics_json) as SpeakingMetrics);
      if (!hasRun) { setStage("intro"); return; }
      const allAnswered = assessment ? assessment.items.every((q) => s.run.answered.includes(q.id)) : true;
      setStage(!allAnswered ? "items" : s.run.writing?.score == null ? "writing" : "speaking");
    }).catch((e: Error) => { setError(e.message); setStage("intro"); });
  };
  useEffect(() => { load(); }, [id]); // eslint-disable-line react-hooks/exhaustive-deps

  if (!assessment || !found) return <p className="text-rose-700">Este módulo ainda não tem avaliação.</p>;
  const { module } = found;
  const backTo = { to: `/modules/${id}`, label: "Voltar ao módulo" };

  const finish = async () => {
    setBusy(true); setError(null); setMissing(null);
    try {
      const res = await api.finishModuleAssessment(id);
      setRecord(res.assessment);
      setStage("result");
    } catch (e) {
      if (e instanceof ApiError && e.status === 409) {
        const body = e.body as { error: string; missing: AssessmentMissing | string[] };
        if (Array.isArray(body.missing)) { load(); } else setMissing(body.missing);
      } else setError((e as Error).message);
    } finally {
      setBusy(false);
    }
  };
  const retake = () => { setRecord(null); setWritingScore(null); setSpoken(null); load(true); };

  if (stage === "loading") return <p className="text-slate-500">Carregando…</p>;
  if (stage === "result" && record) return <AssessmentResult record={record} items={assessment.items} title={assessment.title} backTo={backTo} onRetake={retake} />;
  if (stage === "locked" && state) {
    return (
      <div className="space-y-4">
        <Link to={backTo.to} className="text-sm text-indigo-700 hover:underline">← {module.id}</Link>
        <h1 className="text-2xl font-semibold">{assessment.title}</h1>
        <Card>
          <p className="text-slate-700">Conclua as aulas do módulo antes da avaliação: {state.eligible.lessonsDone}/{state.eligible.lessonsTotal} concluídas.</p>
          <ul className="mt-2 list-disc pl-5 text-sm text-slate-600">{state.eligible.missing.map((l) => <li key={l}><Link to={`/lessons/${l}`} className="text-indigo-700 hover:underline">{l}</Link></li>)}</ul>
        </Card>
      </div>
    );
  }

  const answered = state?.run.answered ?? [];
  const body = (() => {
    switch (stage) {
      case "intro":
        return (
          <div className="space-y-4">
            <Markdown text={assessment.intro} />
            <p className="text-sm text-slate-500">{assessment.items.length} itens · 1 escrita · 1 gravação. Pode fechar e retomar; as respostas ficam salvas.</p>
            <Button onClick={() => setStage("items")}>Começar</Button>
          </div>
        );
      case "items":
        return <ExerciseList key="items" lessonId={id} block="assessment" exercises={assessment.items} feedback="deferred" initialAnswered={answered} onFinished={() => setStage("writing")} />;
      case "writing":
        return (
          <div className="space-y-4">
            <Writing spec={assessment.writing} fetchLatest={() => api.moduleAssessmentState(id).then((s) => s.run.writing)} submit={(b) => api.submitModuleWriting(id, b)} onSaved={setWritingScore} />
            <p className="text-sm text-slate-600">Envie o texto, compare com o modelo e salve sua nota (1–5) para continuar.</p>
            <Button disabled={writingScore === null} onClick={() => setStage("speaking")}>Continuar para a fala →</Button>
          </div>
        );
      case "speaking":
        return (
          <div className="space-y-6">
            <Speaking spec={assessment.speaking} title="Gravação" onSubmitted={setSpoken} submit={(b) => api.submitModuleSpeaking(id, { mode: "A", ...b })} />
            <Button disabled={busy || !spoken} onClick={finish}>Concluir a avaliação</Button>
            {!spoken && <p className="text-xs text-slate-500">A gravação é obrigatória na avaliação (nota mínima {content.modules[id]?.pass.speakingMin ?? 3}/5).</p>}
          </div>
        );
      default:
        return null;
    }
  })();

  return (
    <div className="space-y-5">
      <Link to={backTo.to} className="text-sm text-indigo-700 hover:underline">← {module.id}</Link>
      <h1 className="text-2xl font-semibold">{assessment.title}</h1>
      {stage !== "intro" && <Stepper steps={STEPS} current={Math.max(0, STEPS.findIndex((s) => s.key === stage))} onSelect={() => undefined} readOnly />}
      {error && <p className="text-sm text-rose-700">Servidor não respondeu ({error}).</p>}
      {missing && (
        <div className="rounded-md border border-amber-300 bg-amber-50 p-3 text-sm text-amber-900">
          Faltam: {[missing.exercises.length > 0 ? `${missing.exercises.length} item(ns)` : "", missing.writing ? "a nota da escrita" : "", missing.speaking ? "a gravação" : ""].filter(Boolean).join(", ")}.
          {missing.exercises.length > 0 && <Button variant="ghost" onClick={() => load()}>Voltar aos itens</Button>}
          {missing.writing && <Button variant="ghost" onClick={() => setStage("writing")}>Ir para a escrita</Button>}
        </div>
      )}
      <div className="rounded-lg border border-slate-200 bg-white p-6 shadow-sm">{body}</div>
    </div>
  );
}
```

- [ ] **Step 5: Rota, módulo e trilha**

`src/App.tsx`: importe `ModuleAssessment` e adicione `{ path: "modules/:id/assessment", element: <ModuleAssessment /> }` depois de `modules/:id`.

`src/pages/Module.tsx`: use `const { byLesson, modules } = useOverview();` e, depois do `<Card>` de metadados, antes da lista de aulas:

```tsx
      {content.moduleAssessments[module.id] && (() => {
        const withContent = module.lessons.filter((l) => hasContent(l.id));
        const done = withContent.filter((l) => byLesson.get(l.id)?.status === "completed").length;
        const summary = modules[module.id];
        return (
          <Card>
            <div className="flex flex-wrap items-center justify-between gap-3">
              <div>
                <h2 className="font-medium">Avaliação do módulo</h2>
                <p className="text-sm text-slate-600">
                  {summary?.latest
                    ? `${summary.passed ? "Aprovado" : "Reprovado"} · itens ${Math.round(summary.latest.result.itemsPct * 100)}% · ${new Date(summary.latest.ts).toLocaleDateString("pt-BR")}`
                    : done < withContent.length ? `Conclua as ${withContent.length} aulas (${done} feitas).` : "Todas as aulas concluídas. Pode fazer a avaliação."}
                </p>
              </div>
              <Link to={`/modules/${module.id}/assessment`} className={`rounded-md px-4 py-2 text-sm font-medium ${done < withContent.length && !summary?.latest ? "pointer-events-none bg-slate-200 text-slate-500" : "bg-indigo-600 text-white hover:bg-indigo-700"}`}>
                {summary?.latest ? "Ver resultado" : "Fazer avaliação"}
              </Link>
            </div>
          </Card>
        );
      })()}
```

(importe `content` de `../lib/content.ts`).

`src/pages/Levels.tsx`: `const { byLesson, modules, error } = useOverview();` e, na linha dos badges do módulo, antes do ternário existente: `{modules[m.id]?.passed && <Badge tone="green">aprovado</Badge>}` (dentro do mesmo contêiner dos badges; o badge "concluído" continua).

- [ ] **Step 6: Verifique**

Run: `pnpm typecheck && pnpm build && pnpm test`
Expected: PASS (202). Suba `pnpm dev` e confira em `http://localhost:5173/modules/M01` o card da avaliação bloqueado ("Conclua as 1 aulas" hoje; 5 depois das aulas novas) e `http://localhost:5173/modules/M01/assessment` mostrando a tela "locked". Encerre com `make stop-dev`.

- [ ] **Step 7: Commit**

```bash
git add src/lib/api.ts src/lib/useOverview.ts src/App.tsx src/pages/ModuleAssessment.tsx src/components/assessment/AssessmentResult.tsx src/pages/Module.tsx src/pages/Levels.tsx
git commit -m "feat(client): página da avaliação de módulo, card no módulo e badge na trilha"
```

---

### Task 5: Catálogo de erros (`br.actually`) e tag `gram.time-expressions`

**Files:**
- Modify: `content/br-errors.yaml`, `content/tags.yaml`
- Test: `tests/br-detector.test.ts`

**Interfaces:**
- Produces: padrão `actually-currently` (tag `br.actually`) no catálogo; tag `gram.time-expressions` em `tags.yaml` (usada por M01-03 e pela avaliação).

- [ ] **Step 1: Testes que falham** — em `tests/br-detector.test.ts`:

```ts
  it("flags 'actually' used as 'currently' only with an explicit present-time context", () => {
    expect(tagsOf("Actually I'm working at a bank now.")).toContain("br.actually");
    expect(tagsOf("Actually we are using Terraform these days.")).toContain("br.actually");
    expect(tagsOf("I actually work at Nubank nowadays.")).toContain("br.actually");
  });
  it("does NOT flag the correct uses of 'actually' or of 'depend on'", () => {
    const clean = [
      "Actually I'm not sure that's right.",
      "Actually, yeah, Marcos can pair with me.",
      "It's actually working now.",
      "Actually I finished it yesterday.",
      "It depends on the runner.",
      "The dependency of the module is outdated.",
    ];
    for (const s of clean) expect(detectBrErrors(s, patterns), s).toEqual([]);
    expect(tagsOf("It depends of the runner.")).toContain("br.depend-of");
  });
```

- [ ] **Step 2: Rode para ver falhar**

Run: `pnpm test tests/br-detector.test.ts`
Expected: FAIL — `br.actually` não é detectado.

- [ ] **Step 3: `content/br-errors.yaml`** — acrescente ao final de `patterns`:

```yaml
  - id: actually-currently
    pattern: "\\bactually,? (i|we|he|she|they)\\s*('m|am|'re|are|'s|is)\\s+\\w+ing\\b[^.!?]{0,40}\\b(now|nowadays|these days|at the moment|currently|right now)\\b|\\b(i|we|they) actually (work|works|use|uses|run|runs|live|lives|study|studies)\\b[^.!?]{0,40}\\b(now|nowadays|these days|at the moment|currently|right now)\\b"
    wrong: "Actually I'm working at a bank now"
    right: "Currently I'm working at a bank / I'm working at a bank now"
    why: "actually = na verdade (correção ou surpresa). Atualmente é currently / nowadays / at the moment."
    tag: br.actually
```

`content/tags.yaml`, na seção de gramática, depois de `gram.by-until`:

```yaml
  - { id: gram.time-expressions,       group: gram, label: Expressões de tempo e prazo }
```

- [ ] **Step 4: Verifique**

Run: `pnpm content:build && pnpm test && pnpm typecheck`
Expected: PASS (204). O teste de guarda existente ("Actually I'm not sure that's right.") continua verde. Se "It's actually working now." disparar, o padrão está largo: ele exige sujeito pessoal (i/we/he/she/they) logo depois de "actually" — "it" fica de fora de propósito.

- [ ] **Step 5: Commit**

```bash
git add content/br-errors.yaml content/tags.yaml tests/br-detector.test.ts
git commit -m "content(br-errors): padrão conservador para actually = atualmente; tag gram.time-expressions"
```

---

### Task 6: Aula M01-01 — Anatomia de um standup

**Files:**
- Create: `content/modules/M01/lessons/M01-01.yaml`, `tests/lessons-m01.test.ts`

**Interfaces:**
- Consumes: `LessonSchema` (inalterado), tags de `content/tags.yaml`, motores `ruleBasedFeedback`/`wordOverlap` para os testes.
- Produces: aula M01-01 jogável; `tests/lessons-m01.test.ts` com uma função `checkLesson(id, expectations)` reutilizada pelas T7–T9 (cada uma acrescenta um `it`).

- [ ] **Step 1: Teste que falha** — crie `tests/lessons-m01.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { loadContent, crossValidate, allExercises } from "../shared/content-loader.ts";
import { ruleBasedFeedback } from "../server/writing-feedback.ts";
import { wordOverlap } from "../shared/speech-compare.ts";
import { parseMarkdown } from "../shared/mini-markdown.ts";
import type { Lesson } from "../shared/schema.ts";

const bundle = loadContent("content");

type Expect = { vocabMin: number; dialogueMin: number; listening: number; quiz: number; cardsMin: number; maxSeconds: number; prerequisites: string[] };

/** Checklist de qualidade da spec §4.1, aplicada a cada aula nova do M01. */
function checkLesson(id: string, e: Expect) {
  const lesson: Lesson = bundle.lessons[id]!;
  it(`${id} exists in the bundle and follows the roadmap`, () => {
    expect(lesson).toBeDefined();
    expect(lesson.module).toBe("M01");
    expect(lesson.prerequisites).toEqual(e.prerequisites);
    expect(lesson.completion).toEqual({ quizMin: 0.75, writingMin: 3, speakingRequired: true });
  });
  it(`${id} has the required block sizes`, () => {
    expect(lesson.vocabulary.length).toBeGreaterThanOrEqual(e.vocabMin);
    expect(lesson.vocabulary.length).toBeLessThanOrEqual(16);
    expect(lesson.examples.length).toBeGreaterThanOrEqual(6);
    expect(lesson.variations.length).toBeGreaterThanOrEqual(2);
    expect(lesson.brErrors.length).toBeGreaterThanOrEqual(8);
    expect(lesson.dialogue.lines.length).toBeGreaterThanOrEqual(e.dialogueMin);
    expect(lesson.listening.questions).toHaveLength(e.listening);
    expect(lesson.quiz).toHaveLength(e.quiz);
    expect(lesson.srsCards.length).toBeGreaterThanOrEqual(e.cardsMin);
    expect(lesson.srsCards.length).toBeLessThanOrEqual(12);
    expect(lesson.speaking.modeA.targetPhrases.length).toBeGreaterThanOrEqual(12);
    expect(lesson.speaking.modeA.maxSeconds).toBe(e.maxSeconds);
    expect(lesson.writing.constraints.length).toBeGreaterThanOrEqual(3);
    expect(lesson.review.preferTags.length).toBeGreaterThanOrEqual(3);
  });
  it(`${id} quiz mixes at least 4 exercise types and balances multiple-choice answers`, () => {
    expect(new Set(lesson.quiz.map((q) => q.type)).size).toBeGreaterThanOrEqual(4);
    const mc = allExercises(lesson).map((x) => x.exercise).filter((q) => q.type === "multiple_choice");
    const counts = new Map<number, number>();
    for (const q of mc) if (q.type === "multiple_choice") counts.set(q.answer, (counts.get(q.answer) ?? 0) + 1);
    const max = Math.max(0, ...counts.values());
    expect(max, `distribuição ${JSON.stringify([...counts])}`).toBeLessThanOrEqual(Math.max(1, Math.floor(mc.length * 0.5)));
  });
  it(`${id} writing model satisfies its constraints and length with no BR errors`, () => {
    const fb = ruleBasedFeedback(lesson.writing.model, lesson.writing, bundle.brErrors);
    expect(fb.withinLength).toBe(true);
    expect(fb.constraints.filter((c) => c.met !== null).every((c) => c.met), JSON.stringify(fb.constraints)).toBe(true);
    expect(fb.findings).toEqual([]);
  });
  it(`${id} target phrases, dialogue and cards are clean`, () => {
    for (const p of lesson.speaking.modeA.targetPhrases) expect(wordOverlap(p, p)).toBe(1);
    const fronts = lesson.srsCards.map((c) => c.front.toLowerCase());
    expect(new Set(fronts).size).toBe(fronts.length);
    for (const line of lesson.dialogue.lines) expect(line.text.trim().length).toBeGreaterThan(0);
  });
  it(`${id} markdown fields render without leftover asterisks`, () => {
    const fields = [lesson.context.scenario, lesson.writing.prompt, lesson.grammar?.explanation ?? ""];
    for (const text of fields) {
      for (const block of parseMarkdown(text)) {
        const inlines = block.type === "paragraph" ? block.inlines : block.type === "list" ? block.items.flat() : [...block.header.flat(), ...block.rows.flat(2)];
        for (const inline of inlines) expect(inline.text, text.slice(0, 40)).not.toContain("*");
      }
    }
  });
}

describe("M01 lessons", () => {
  it("passes cross-validation", () => {
    expect(crossValidate(bundle)).toEqual([]);
  });
  checkLesson("M01-01", { vocabMin: 12, dialogueMin: 6, listening: 6, quiz: 8, cardsMin: 10, maxSeconds: 45, prerequisites: [] });
});
```

- [ ] **Step 2: Rode para ver falhar**

Run: `pnpm test tests/lessons-m01.test.ts`
Expected: FAIL — `bundle.lessons["M01-01"]` undefined.

- [ ] **Step 3: Crie `content/modules/M01/lessons/M01-01.yaml`** exatamente com:

```yaml
id: M01-01
module: M01
order: 1
title: "Anatomia de um standup: yesterday / today / blockers"
objective: >-
  Ao final, você consegue dar um update de 30 segundos com as três partes na ordem (ontem, hoje, bloqueios),
  usando frases-molde que funcionam em qualquer time, sem entrar em detalhe técnico demais, sem pedir desculpa
  e sem dizer "everything is fine" quando não está.
durationMin: 35
competencies: [SPK, VOC, LIS]
tags: [topic.daily, vocab.standup, gram.future-plans, gram.present-perfect, br.actually, br.pretend, comp.speaking]
prerequisites: []

context:
  scenario: |
    Você acabou de entrar num time de plataforma distribuído (Brasil, Irlanda, Índia). O daily é às 9h30 BRT, 15 minutos, 6 pessoas, no Google Meet. Quem conduz é a Priya, scrum master. A regra da casa: cada pessoa fala **menos de um minuto** e segue a ordem *yesterday / today / blockers*.

    Ontem você configurou o `kubectl` e o acesso ao cluster de staging, leu o runbook de deploy e abriu um PR pequeno corrigindo um typo no Helm chart do `notifications-api`. Hoje vai acompanhar o Marcos no deploy de staging e começar a ler o código do serviço. Não tem bloqueio, mas ainda não recebeu acesso ao Grafana.

    É o seu primeiro standup em inglês nesse time. Você não precisa impressionar; precisa ser claro, curto e fácil de acompanhar.
  roles: [Priya (scrum master), Marcos (platform engineer), Ana (SRE)]

vocabulary:
  - term: Yesterday I…
    meaning: abre a primeira parte do update (o que foi feito)
    example: Yesterday I set up kubectl and got access to the staging cluster.
    translation: Ontem configurei o kubectl e consegui acesso ao cluster de staging.
    note: Past simple. Uma ou duas frases bastam.
  - term: Today I'm going to… / Today I'll…
    meaning: abre a segunda parte (o plano de hoje)
    example: Today I'm going to shadow Marcos on the staging deploy.
    translation: Hoje vou acompanhar o Marcos no deploy de staging.
    note: '"going to" para plano decidido; "I''ll" para decisão no momento ou promessa.'
  - term: No blockers.
    meaning: fecha o update quando nada impede o trabalho
    example: No blockers.
    translation: Sem bloqueios.
    note: Diga sempre, mesmo curto. O silêncio deixa a dúvida no ar.
  - term: One thing:
    meaning: introduz um pedido ou aviso pequeno depois do update
    example: "One thing: I still don't have access to Grafana."
    translation: "Uma coisa: ainda não tenho acesso ao Grafana."
    note: Alternativa a "quick one".
  - term: I set up X
    meaning: configurei / preparei X
    example: I set up my local environment and the VPN.
    translation: Configurei meu ambiente local e a VPN.
    note: '"set up" (verbo, duas palavras) × "setup" (substantivo).'
  - term: I got access to X
    meaning: consegui acesso a X
    example: I got access to the staging cluster.
    translation: Consegui acesso ao cluster de staging.
    note: '"get access" é a colocação natural; "receive access" soa formal.'
  - term: I opened a PR (for/to…)
    meaning: abri um pull request
    example: I opened a small PR to fix a typo in the Helm chart.
    translation: Abri um PR pequeno corrigindo um typo no chart do Helm.
    note: '"open" ou "raise" a PR; "make a PR" soa não-nativo.'
  - term: I read through X
    meaning: li X do começo ao fim
    example: I read through the deploy runbook.
    translation: Li o runbook de deploy inteiro.
    note: '"read through" = leitura completa; "skimmed" = leitura rápida.'
  - term: I'm going to shadow X on Y
    meaning: vou acompanhar alguém fazendo algo, para aprender
    example: I'm going to shadow Marcos on the staging deploy.
    translation: Vou acompanhar o Marcos no deploy de staging.
    note: Comum em onboarding e on-call.
  - term: I'll start looking into X
    meaning: vou começar a investigar / estudar X
    example: I'll start looking into the notifications-api codebase.
    translation: Vou começar a olhar o código do notifications-api.
    note: '"look into" = investigar. Sem "the code of…" desnecessário.'
  - term: I'm picking up X
    meaning: estou assumindo a tarefa X
    example: I'm picking up the alerting ticket.
    translation: Estou pegando o ticket de alertas.
    note: Sinaliza que a tarefa tem dono.
  - term: Nothing else from me.
    meaning: encerra e passa a vez
    example: That's it, nothing else from me.
    translation: É isso, nada mais da minha parte.
    note: Marca o fim; evita a pausa constrangedora.
  - term: Still no access to X
    meaning: continuo sem acesso a X (aviso, não drama)
    example: Still no access to Grafana, but it's not blocking me yet.
    translation: Ainda sem acesso ao Grafana, mas ainda não me bloqueia.
    note: '"not blocking me yet" avisa cedo sem pedir socorro.'
    register: informal
  - term: That's it from me.
    meaning: encerramento neutro
    example: That's it from me.
    translation: É isso da minha parte.
    note: Igual a "nothing else from me".

grammar:
  title: Past simple para ontem, going to / will para hoje
  explanation: |
    O standup tem dois tempos. **Ontem** é fechado: past simple. **Hoje** é plano: *going to* (decidido) ou *will* (decisão na hora, promessa).

    | Parte | Forma | Exemplo |
    |---|---|---|
    | Yesterday | past simple | Yesterday I **set up** kubectl and **opened** a PR. |
    | Today (plano) | be going to | Today I**'m going to** shadow Marcos on the deploy. |
    | Today (decisão agora) | will | I**'ll** ping you after the call. |
    | Em andamento desde antes | present perfect continuous | I**'ve been reading** the runbook since yesterday. |

    **Três armadilhas de brasileiro:**

    - "Actually I'm working on X" para dizer "atualmente": *actually* = na verdade. Use *currently* ou *now*.
    - "I pretend to finish today": *pretend* = fingir. Use *I plan to* / *I intend to*.
    - "Today I do the deploy": presente simples soa como rotina. Para plano de hoje, *I'm going to* / *I'll*.

    **O que não dizer no standup:** detalhe técnico demais (guarde para depois da call), desculpas em série ("sorry, sorry"), e "everything is fine" quando existe um risco. O daily serve para o time enxergar o que está acontecendo.
  examples:
    - { en: "Yesterday I set up my environment.", pt: "Ontem configurei meu ambiente." }
    - { en: "Today I'm going to read the runbook.", pt: "Hoje vou ler o runbook." }
    - { en: "I'll send the link after the call.", pt: "Mando o link depois da call." }
    - { en: "I've been reading the code since yesterday.", pt: "Estou lendo o código desde ontem." }

examples:
  - en: Yesterday I set up kubectl and got access to staging. Today I'm going to shadow Marcos on the deploy. No blockers.
    pt: Ontem configurei o kubectl e consegui acesso ao staging. Hoje vou acompanhar o Marcos no deploy. Sem bloqueios.
    context: Update completo de 12 segundos. As três partes, na ordem.
  - en: Yesterday I read through the deploy runbook and opened a small PR for a typo in the Helm chart.
    pt: Ontem li o runbook de deploy inteiro e abri um PR pequeno para um typo no chart do Helm.
    context: Duas ações fechadas no passado. Sem explicar o typo.
  - en: Today I'll start looking into the notifications-api codebase.
    pt: Hoje vou começar a olhar o código do notifications-api.
    context: Plano do dia em uma frase; "start looking into" avisa que é leitura, não entrega.
  - en: "One thing: I still don't have access to Grafana. Not blocking me yet."
    pt: "Uma coisa: ainda não tenho acesso ao Grafana. Ainda não me bloqueia."
    context: Aviso pequeno, sem virar bloqueio. Quem cuida de acesso ouve e resolve.
  - en: Nothing else from me.
    pt: Nada mais da minha parte.
    context: Encerramento. Evita a pausa em que ninguém sabe se você terminou.
  - en: I'm picking up the alerting ticket after the deploy.
    pt: Vou pegar o ticket de alertas depois do deploy.
    context: '"picking up" = assumindo. Mostra que a tarefa tem dono e ordem.'
  - en: Currently I'm working on the onboarding tasks.
    pt: Atualmente estou trabalhando nas tarefas de onboarding.
    context: '"currently", não "actually", para "atualmente".'

variations:
  - idea: '"fechar o update"'
    items:
      - { register: Neutro, text: "No blockers.", adequate: true }
      - { register: Neutro, text: "That's it from me.", adequate: true }
      - { register: Informal, text: "Nothing else, thanks!", adequate: true, note: Com colegas próximos. }
      - { register: Inadequada, text: "…", adequate: false, note: Silêncio; ninguém sabe se acabou. }
      - { register: Inadequada, text: "Everything is fine, no problems at all, all good.", adequate: false, note: Excesso; soa como esconder algo. }
  - idea: '"ainda não tenho acesso"'
    items:
      - { register: Neutro (daily), text: "Still no access to Grafana, not blocking me yet.", adequate: true }
      - { register: Formal (e-mail), text: "My Grafana access request is still pending.", adequate: true }
      - { register: Inadequada, text: "Sorry, I don't have access, sorry, I don't know why.", adequate: false, note: Desculpas sem informação; não é culpa sua. }
      - { register: Inadequada, text: "I pretend to ask for access today.", adequate: false, note: '"pretend" = fingir. Diga "I plan to".' }

brErrors:
  - { wrong: "Actually I'm working on onboarding tasks. (= atualmente)", right: "Currently I'm working on onboarding tasks.", why: "actually = na verdade", tag: br.actually }
  - { wrong: "I pretend to finish the setup today.", right: "I plan to finish the setup today.", why: "pretend = fingir", tag: br.pretend }
  - { wrong: "Today I do the deploy with Marcos.", right: "Today I'm going to do the deploy with Marcos.", why: "plano de hoje pede going to / will", tag: gram.future-plans }
  - { wrong: "Yesterday I have configured kubectl.", right: "Yesterday I configured kubectl.", why: "yesterday fecha o momento: past simple", tag: gram.present-perfect }
  - { wrong: "I made a PR yesterday.", right: "I opened a PR yesterday.", why: "PR se abre (open/raise), não se faz", tag: vocab.standup }
  - { wrong: "I have a doubt about the runbook.", right: "I have a question about the runbook.", why: "doubt = desconfiança", tag: br.doubt }
  - { wrong: "I'm waiting the Grafana access.", right: "I'm waiting on/for the Grafana access.", why: "wait exige preposição", tag: br.waiting-no-prep }
  - { wrong: "Sorry, sorry, I'm new, sorry.", right: "I'm new here, so bear with me.", why: "uma vez basta; desculpa em série soa inseguro", tag: topic.daily }
  - { wrong: "I will explain you the setup later.", right: "I'll walk you through the setup later.", why: "explain não aceita objeto indireto direto", tag: br.explain-me }

dialogue:
  title: "Seu primeiro standup no time, 9:30 BRT."
  lines:
    - { speaker: Priya, text: "Morning all. Quick round, one minute each. Yuri, welcome — you go first." }
    - { speaker: Yuri, text: "Thanks. Yesterday I set up kubectl and got access to the staging cluster, read through the deploy runbook, and opened a small PR for a typo in the notifications-api Helm chart. Today I'm going to shadow Marcos on the staging deploy and start looking into the codebase. No blockers. One thing: I still don't have access to Grafana, but it's not blocking me yet.", note: "Três partes, 20 segundos. O aviso do Grafana vem depois de 'no blockers'." }
    - { speaker: Priya, text: "Good. Ana, can you sort out the Grafana access?" }
    - { speaker: Ana, text: "On it. I'll add you to the group right after this." }
    - { speaker: Yuri, text: "Thanks, Ana." }
    - { speaker: Priya, text: "Marcos, you're up." }
    - { speaker: Marcos, text: "Yesterday I finished the node pool upgrade in staging. Today I'm deploying notifications-api with Yuri. No blockers." }
    - { speaker: Priya, text: "Great, keep it that short. Ana?" }
  notes:
    - '"You go first" / "you''re up" = é a sua vez.'
    - '"Sort out" = resolver, providenciar. "On it" = já estou cuidando.'
    - 'O update de Marcos tem 20 palavras e é perfeitamente aceitável: não precisa ser longo para ser bom.'

listening:
  lines:
    - { speaker: Ana, text: "Yesterday I rotated the certificates on the ingress and updated the runbook. Today I'm going to review the alerting rules for the new cluster. No blockers, but heads up: I'll be out from noon my time." }
    - { speaker: Marcos, text: "Yesterday I finished the node pool upgrade in staging; it went fine. Today I'm deploying notifications-api with Yuri after this call. Nothing blocking me." }
    - { speaker: Priya, text: "Quick one: the retro moved to Thursday at 2 pm BRT. I'll send the invite. That's it from me." }
  questions:
    - { id: M01-01-l1, type: multiple_choice, prompt: "What did Ana do yesterday?", options: ["Reviewed the alerting rules", "Rotated the ingress certificates", "Upgraded the node pool", "Moved the retro"], answer: 1, explanation: '"Yesterday I rotated the certificates on the ingress."', tags: [comp.listening] }
    - { id: M01-01-l2, type: multiple_choice, prompt: "Who has a blocker?", options: ["Ana", "Marcos", "Priya", "Nobody"], answer: 3, explanation: 'Ana: "No blockers"; Marcos: "Nothing blocking me"; Priya não fala de bloqueio.', tags: [comp.listening, vocab.standup] }
    - { id: M01-01-l3, type: fill_blank, prompt: "Ana will be out from ___ her time.", accepted: ["noon", "12", "12 pm", "midday"], explanation: '"I''ll be out from noon my time."', tags: [comp.listening] }
    - { id: M01-01-l4, type: multiple_choice, prompt: "What is Marcos going to do today?", options: ["Upgrade the node pool", "Rotate certificates", "Deploy notifications-api with Yuri", "Send the retro invite"], answer: 2, explanation: '"Today I''m deploying notifications-api with Yuri."', tags: [comp.listening] }
    - { id: M01-01-l5, type: multiple_choice, prompt: "When is the retro now?", options: ["Thursday at 2 pm BRT", "Tuesday at noon", "Today after the call", "Thursday at 9:30"], answer: 0, explanation: '"the retro moved to Thursday at 2 pm BRT"', tags: [comp.listening] }
    - { id: M01-01-l6, type: multiple_choice, prompt: '"It went fine" means the upgrade…', options: ["failed", "was cancelled", "worked without problems", "is still running"], answer: 2, explanation: '"went fine" = correu bem.', tags: [vocab.standup] }

writing:
  prompt: |
    **Cenário:** ontem você configurou o ambiente local, conseguiu acesso ao cluster de staging e leu o runbook de deploy. Hoje vai acompanhar a Ana numa rotação de certificados e começar a ler o código do serviço de notificações. Não há bloqueio, mas você ainda não recebeu acesso ao Grafana.

    **Tarefa:** escreva seu update de standup (60–90 palavras) na ordem yesterday / today / blockers, para ser lido em voz alta em menos de 40 segundos.
  constraints:
    - { label: 'Começa com "Yesterday"', pattern: "^\\s*yesterday\\b" }
    - { label: '"Today" + going to / I''ll', pattern: "\\btoday (i'm going to|i am going to|i'll|i will)\\b" }
    - { label: '"No blockers" ou "Nothing blocking me"', pattern: "\\b(no blockers|nothing blocking me)\\b" }
    - { label: "Aviso do Grafana em uma frase" }
  rubric:
    - Estrutura yesterday / today / blockers
    - Tempos verbais (past simple × going to / will)
    - Concisão (nada de detalhe técnico)
    - Naturalidade e registro
  model: >-
    Yesterday I set up my local environment, got access to the staging cluster and read through the deploy
    runbook. Today I'm going to shadow Ana on the certificate rotation and then start looking into the
    notifications service codebase, mostly reading for now. No blockers. One thing: I still don't have access to Grafana, but it's not
    blocking me yet. That's it from me.
  minWords: 60
  maxWords: 90
  tags: [comp.writing, topic.daily, gram.future-plans]

speaking:
  modeA:
    prompt: Grave seu update de standup de hoje (real ou o do cenário) em até 45 segundos, na ordem yesterday / today / blockers. Use pelo menos 4 expressões da aula.
    maxSeconds: 45
    targetPhrases:
      - yesterday I
      - today I'm going to
      - today I'll
      - I set up
      - I got access to
      - I opened a PR
      - I read through
      - shadow
      - start looking into
      - I'm picking up
      - no blockers
      - nothing blocking me
      - one thing
      - not blocking me yet
      - that's it from me
      - nothing else from me
    checklist:
      - Comecei com "Yesterday" e uma ação fechada?
      - Usei "going to" ou "I'll" para o plano de hoje?
      - Fechei com "no blockers" ou um aviso curto?
      - Fiquei abaixo de 45 segundos sem detalhe técnico?

quiz:
  - { id: M01-01-q1, type: reorder, prompt: "Coloque as partes do update na ordem certa.", tokens: ["blockers", "today", "yesterday"], answer: "yesterday today blockers", explanation: "Ordem fixa do standup: o que fiz, o que vou fazer, o que me trava.", tags: [topic.daily, vocab.standup] }
  - { id: M01-01-q2, type: multiple_choice, prompt: "Yesterday I ___ the VPN.", options: ["have set up", "set up", "am setting up", "setup"], answer: 1, explanation: "yesterday fecha o momento: past simple. \"setup\" junto é substantivo.", tags: [gram.present-perfect, vocab.standup] }
  - { id: M01-01-q3, type: error_correction, prompt: "Actually I'm working on the onboarding tasks now.", accepted: ["Currently I'm working on the onboarding tasks.", "Currently I'm working on the onboarding tasks now.", "I'm currently working on the onboarding tasks.", "Right now I'm working on the onboarding tasks.", "I'm working on the onboarding tasks now."], explanation: "actually = na verdade. Atualmente é currently / now / at the moment.", tags: [br.actually] }
  - { id: M01-01-q4, type: fill_blank, prompt: "Today I'm ___ to shadow Marcos on the deploy.", accepted: ["going"], explanation: "Plano decidido: be going to.", tags: [gram.future-plans] }
  - { id: M01-01-q5, type: multiple_choice, prompt: "Which closing is the best?", options: ["…", "Everything is fine, no problems at all, all good.", "No blockers. That's it from me.", "Sorry, that's all, sorry."], answer: 2, explanation: "Curto, claro e marca o fim. Silêncio deixa dúvida; excesso soa como esconder algo; desculpa sem motivo soa inseguro.", tags: [topic.daily, vocab.standup] }
  - { id: M01-01-q6, type: error_correction, prompt: "I pretend to read the runbook today.", accepted: ["I plan to read the runbook today.", "I intend to read the runbook today.", "I'm going to read the runbook today.", "I plan to read the runbook today", "I intend to read the runbook today"], explanation: "pretend = fingir. Pretender é plan to / intend to.", tags: [br.pretend] }
  - { id: M01-01-q7, type: translate, prompt: "Abri um PR pequeno ontem.", accepted: ["I opened a small PR yesterday.", "I opened a small PR yesterday", "Yesterday I opened a small PR.", "Yesterday I opened a small PR", "I raised a small PR yesterday.", "I raised a small PR yesterday"], explanation: "open / raise a PR. \"make a PR\" soa não-nativo.", tags: [vocab.standup, vocab.tickets-prs] }
  - { id: M01-01-q8, type: match, prompt: "Associe a parte do update à frase.", pairs: [{ left: "yesterday", right: "I rotated the ingress certificates." }, { left: "today", right: "I'm going to review the alerting rules." }, { left: "blockers", right: "Still no access to Grafana." }], explanation: "Cada parte tem um tempo verbal: past simple, going to, presente.", tags: [topic.daily] }

review:
  count: 5
  preferTags: [gram.question-forms, br.doubt, gram.future-plans]

srsCards:
  - { front: "ontem eu configurei o kubectl", back: "Yesterday I set up kubectl", tag: vocab.standup }
  - { front: "consegui acesso ao cluster de staging", back: "I got access to the staging cluster", tag: vocab.standup }
  - { front: "abri um PR pequeno", back: "I opened a small PR", tag: vocab.tickets-prs }
  - { front: "li o runbook inteiro", back: "I read through the runbook", tag: vocab.standup }
  - { front: "hoje vou acompanhar o Marcos no deploy", back: "Today I'm going to shadow Marcos on the deploy", tag: gram.future-plans }
  - { front: "vou começar a olhar o código", back: "I'll start looking into the codebase", tag: gram.future-plans }
  - { front: "estou pegando o ticket de alertas", back: "I'm picking up the alerting ticket", tag: vocab.standup }
  - { front: "sem bloqueios", back: "No blockers", tag: vocab.standup }
  - { front: "uma coisa: ainda não tenho acesso ao Grafana", back: "One thing: I still don't have access to Grafana", tag: topic.daily }
  - { front: "ainda não me bloqueia", back: "It's not blocking me yet", tag: vocab.standup }
  - { front: "é isso da minha parte", back: "That's it from me", tag: vocab.standup }
  - { front: "atualmente (NÃO \"actually\")", back: "currently / at the moment", tag: br.actually }

completion:
  quizMin: 0.75
  writingMin: 3
  speakingRequired: true
```

- [ ] **Step 4: Verifique**

Run: `pnpm content:build && pnpm test && pnpm typecheck`
Expected: `gerado src/generated/content.json (2 aula(s) com conteúdo)`; PASS (204 + 7 = 211). Se `crossValidate` acusar significado repetido com o glossário (`meaning` de vocabulário × `daily.yaml`), ajuste o `meaning` da aula (não o glossário) e relate. Se o modelo de escrita disparar algum padrão do catálogo, ajuste o modelo.

- [ ] **Step 5: Commit**

```bash
git add content/modules/M01/lessons/M01-01.yaml tests/lessons-m01.test.ts
git commit -m "content(M01): aula M01-01 — anatomia de um standup"
```

---

### Task 7: Aula M01-03 — Tempo e prazos

**Files:**
- Create: `content/modules/M01/lessons/M01-03.yaml`
- Modify: `tests/lessons-m01.test.ts` (1 linha)

**Interfaces:**
- Consumes: `checkLesson` (T6); tag `gram.time-expressions` (T5).
- Produces: aula M01-03 jogável.

- [ ] **Step 1: Teste que falha** — em `tests/lessons-m01.test.ts`, dentro de `describe("M01 lessons")`, depois da linha da M01-01:

```ts
  checkLesson("M01-03", { vocabMin: 12, dialogueMin: 6, listening: 6, quiz: 8, cardsMin: 10, maxSeconds: 45, prerequisites: ["M01-02"] });
```

Run: `pnpm test tests/lessons-m01.test.ts` → FAIL (`M01-03` undefined).

- [ ] **Step 2: Crie `content/modules/M01/lessons/M01-03.yaml`** exatamente com:

```yaml
id: M01-03
module: M01
order: 3
title: "Tempo e prazos: ETA, EOD, by × until, within"
objective: >-
  Ao final, você consegue dar e pedir prazos com precisão: distingue by (prazo) de until (duração) e within
  (janela), usa ETA, EOD e COB sem tradução literal, e escolhe entre "should be done by", "aiming for" e
  "no later than" conforme a confiança que tem no prazo.
durationMin: 40
competencies: [SPK, VOC, LIS]
tags: [topic.daily, gram.by-until, gram.time-expressions, br.until-by, br.until-late, br.lose-the-deadline, vocab.standup, comp.speaking]
prerequisites: [M01-02]

context:
  scenario: |
    Sexta-feira, daily das 10h BRT. O time combinou entregar a migração dos dashboards do Grafana para o novo cluster **até quarta**. Você está no meio: os dashboards de infra já foram, os de aplicação dependem de uma datasource que o time de dados só libera na terça.

    Priya quer saber uma data. Marcos pergunta se dá para adiantar o corte dos alertas. Ana precisa que você termine **antes** do release de quinta, porque ela vai desligar o cluster antigo.

    Você precisa dizer três coisas com clareza: o que está pronto, uma previsão honesta (com a confiança certa) e o que acontece se a datasource atrasar. Sem prometer o que não controla, sem soar vago.
  roles: [Priya (scrum master), Ana (SRE), Marcos (platform engineer)]

vocabulary:
  - term: ETA
    meaning: previsão de quando algo fica pronto
    example: What's the ETA on the dashboards? — Wednesday, if the datasource lands on Tuesday.
    translation: Qual a previsão dos dashboards? — Quarta, se a datasource chegar na terça.
    note: 'Fala-se letra a letra. "an ETA on/for X"; o horário é "an ETA of 4 pm".'
  - term: EOD / COB
    meaning: fim do dia de trabalho (end of day / close of business)
    example: I'll have the infra dashboards done by EOD.
    translation: Termino os dashboards de infra até o fim do dia.
    note: Sempre "by EOD", nunca "until EOD". COB é mais formal e comum nos EUA.
  - term: by (Wednesday)
    meaning: prazo final (no máximo até)
    example: The migration should be done by Wednesday.
    translation: A migração deve estar pronta até quarta.
    note: Ponto no tempo. É o que você quer dizer quase sempre com "até".
  - term: until (Wednesday)
    meaning: duração (durante o tempo todo até)
    example: I'll be on the dashboards until Wednesday.
    translation: Vou ficar nos dashboards até quarta.
    note: Descreve o período, não o prazo. "I'll finish until Wednesday" está errado.
  - term: within (two days)
    meaning: dentro de uma janela de tempo
    example: I can move the alert cutover within two days of the datasource landing.
    translation: Consigo virar os alertas em até dois dias depois de a datasource chegar.
    note: Janela relativa, não data fixa.
  - term: should be done by
    meaning: previsão em que você confia
    example: The app dashboards should be done by Wednesday.
    translation: Os dashboards de aplicação devem estar prontos até quarta.
    note: '"should" = confiança razoável, sem garantia.'
  - term: I'm aiming for (Wednesday)
    meaning: meta com risco assumido
    example: I'm aiming for Wednesday, but it depends on the datasource.
    translation: Minha meta é quarta, mas depende da datasource.
    note: '"aiming for" + data; "aiming to" + verbo.'
  - term: no later than
    meaning: prazo duro, sem margem
    example: Ana needs everything migrated no later than Wednesday EOD.
    translation: A Ana precisa de tudo migrado até quarta no fim do dia, sem falta.
    note: Formal-neutro; use quando o prazo é de outra pessoa.
  - term: at the latest
    meaning: no máximo (data limite)
    example: I'll know by Tuesday morning at the latest.
    translation: Vou saber até terça de manhã, no máximo.
    note: Vem no fim da frase.
  - term: on track (for)
    meaning: dentro do previsto
    example: We're on track for Wednesday.
    translation: Estamos dentro do prazo para quarta.
    note: Oposto: "behind schedule".
  - term: behind schedule / running late
    meaning: atrasado em relação ao plano
    example: The app dashboards are running a day late.
    translation: Os dashboards de aplicação estão um dia atrasados.
    note: '"running late" = atrasando; "a day late" quantifica.'
  - term: push (something) back / bring (something) forward
    meaning: adiar / adiantar uma data
    example: Can we push the cutover back to Thursday? — I'd rather bring the infra part forward.
    translation: Podemos adiar a virada para quinta? — Prefiro adiantar a parte de infra.
    note: '"push back" adia; "bring forward" adianta.'
  - term: miss the deadline
    meaning: perder o prazo
    example: If the datasource slips, we'll miss the Wednesday deadline.
    translation: Se a datasource atrasar, perdemos o prazo de quarta.
    note: '"lose the deadline" é calque; prazo se perde com miss.'
  - term: I'll know more by (Tuesday)
    meaning: terei mais informação até tal data
    example: I'll know more by Tuesday morning.
    translation: Vou saber mais até terça de manhã.
    note: Dá uma data para a próxima atualização; evita a pergunta "e agora?".
  - term: worst case
    meaning: no pior cenário
    example: Worst case, the app dashboards land on Thursday morning.
    translation: No pior caso, os dashboards de aplicação chegam quinta de manhã.
    note: Sem "in the": "worst case, …". Par: "best case".

grammar:
  title: "by × until × within: prazo, duração e janela"
  explanation: |
    Três palavras que o português junta em "até":

    | Palavra | Ideia | Pergunta que responde | Exemplo |
    |---|---|---|---|
    | **by** | prazo: no máximo até um ponto | *Quando tem que estar pronto?* | I'll have it done **by** Wednesday. |
    | **until** | duração: durante todo o tempo até | *Até quando você fica nisso?* | I'll be on this **until** Wednesday. |
    | **within** | janela: dentro de um período | *Em quanto tempo?* | I'll do it **within** two days. |

    **Teste rápido:** se a frase é sobre *terminar* ou *entregar*, é **by**. "I will finish until Friday" não existe em inglês.

    **Confiança no prazo, do mais firme ao mais solto:**

    - *no later than* Wednesday → prazo duro
    - *should be done by* Wednesday → confiança razoável
    - *I'm aiming for* Wednesday → meta com risco
    - *worst case,* Thursday → cenário de contingência

    **Horas e dias:** *by 4 pm*, *by EOD*, *by Friday*, *by the end of the week*. Com fuso: *by EOD my time* / *by 4 pm BRT*.

    **Dois calques:** "until late" (não existe: *late* já basta, "I stayed late") e "lose the deadline" (é *miss the deadline*).
  examples:
    - { en: "I'll have it done by Wednesday.", pt: "Vou ter isso pronto até quarta." }
    - { en: "I'll be on this until Wednesday.", pt: "Vou ficar nisso até quarta." }
    - { en: "I can do it within two days.", pt: "Consigo fazer em até dois dias." }
    - { en: "We'll miss the deadline if the datasource slips.", pt: "Vamos perder o prazo se a datasource atrasar." }
    - { en: "I stayed late to finish the infra dashboards.", pt: "Fiquei até tarde para terminar os dashboards de infra." }

examples:
  - en: The infra dashboards are done. The app dashboards should be done by Wednesday, assuming the datasource lands on Tuesday.
    pt: Os dashboards de infra estão prontos. Os de aplicação devem estar prontos até quarta, supondo que a datasource chegue na terça.
    context: Previsão honesta com a condição explícita.
  - en: What's the ETA on the alert cutover? — Within a day of the app dashboards, so Thursday morning worst case.
    pt: Qual a previsão da virada dos alertas? — Até um dia depois dos dashboards de aplicação, então quinta de manhã no pior caso.
    context: ETA pedido e dado com janela e pior caso.
  - en: I'm aiming for Wednesday, but I'll know more by Tuesday morning.
    pt: Minha meta é quarta, mas vou saber mais até terça de manhã.
    context: Meta com risco e data da próxima informação.
  - en: Ana needs the old cluster gone no later than Thursday, so Wednesday EOD is a hard deadline for me.
    pt: A Ana precisa do cluster antigo desligado até quinta, sem falta; então quarta no fim do dia é prazo duro para mim.
    context: Prazo de outra pessoa vira o seu.
  - en: Can we push the cutover back a day? I'd rather not rush it on release day.
    pt: Podemos adiar a virada um dia? Prefiro não fazer isso correndo no dia do release.
    context: Pedir para adiar com motivo.
  - en: We're on track for Wednesday. Nothing blocking me right now.
    pt: Estamos dentro do prazo para quarta. Nada me bloqueando agora.
    context: Update curto quando está tudo certo.
  - en: I stayed late yesterday to get the infra part done, so that piece is off the table.
    pt: Fiquei até tarde ontem para terminar a parte de infra, então isso está resolvido.
    context: '"stayed late", não "until late". "off the table" = resolvido / fora de discussão.'

variations:
  - idea: '"vai ficar pronto até quarta"'
    items:
      - { register: Prazo duro, text: "It'll be done no later than Wednesday.", adequate: true }
      - { register: Confiança razoável, text: "It should be done by Wednesday.", adequate: true }
      - { register: Meta com risco, text: "I'm aiming for Wednesday.", adequate: true }
      - { register: Inadequada, text: "I will finish it until Wednesday.", adequate: false, note: '"until" descreve duração; prazo é "by".' }
      - { register: Inadequada, text: "Maybe Wednesday, maybe not, we'll see.", adequate: false, note: Vago; não dá para planejar em cima disso. }
  - idea: '"vai atrasar"'
    items:
      - { register: Neutro, text: "This is running a day late; new ETA is Thursday.", adequate: true }
      - { register: Com causa, text: "The datasource slipped, so the app dashboards will land Thursday instead of Wednesday.", adequate: true }
      - { register: Inadequada, text: "We will lose the deadline.", adequate: false, note: Prazo se perde com "miss". }
      - { register: Inadequada, text: "I don't know when, it depends of a lot of things.", adequate: false, note: '"depends on"; e dê pelo menos um pior caso.' }

brErrors:
  - { wrong: "I will finish the dashboards until Wednesday.", right: "I'll have the dashboards done by Wednesday.", why: "until = duração; prazo é by", tag: br.until-by }
  - { wrong: "I'll be ready until 4 pm.", right: "I'll be ready by 4 pm.", why: "ready + prazo pede by", tag: br.until-by }
  - { wrong: "I stayed until late to finish it.", right: "I stayed late to finish it.", why: '"until late" não existe', tag: br.until-late }
  - { wrong: "We lost the deadline.", right: "We missed the deadline.", why: "prazo se perde com miss", tag: br.lose-the-deadline }
  - { wrong: "The ETA is until Wednesday.", right: "The ETA is Wednesday.", why: "ETA já é um ponto no tempo; sem until", tag: gram.time-expressions }
  - { wrong: "I'll do it in two days. (querendo dizer 'em até dois dias')", right: "I'll do it within two days.", why: "in two days = daqui a dois dias; within = dentro de dois dias", tag: gram.time-expressions }
  - { wrong: "It depends of the datasource.", right: "It depends on the datasource.", why: "depend on", tag: br.depend-of }
  - { wrong: "I'm aiming to Wednesday.", right: "I'm aiming for Wednesday.", why: "aiming for + data; aiming to + verbo", tag: gram.time-expressions }
  - { wrong: "Give me an ETA of the migration.", right: "Give me an ETA on the migration.", why: "ETA on/for + assunto; ETA of + horário", tag: gram.time-expressions }

dialogue:
  title: "Daily de sexta, 10:00 BRT. O prazo da migração dos dashboards é quarta."
  lines:
    - { speaker: Priya, text: "Yuri, where are we on the Grafana migration? I need a date for the release notes." }
    - { speaker: Yuri, text: "The infra dashboards are done as of yesterday. The app dashboards depend on the new datasource, which the data team is releasing on Tuesday. So the app part should be done by Wednesday, and I'll know more by Tuesday morning at the latest.", note: "Pronto / dependência / previsão com condição / data da próxima informação." }
    - { speaker: Marcos, text: "Any chance we can bring the alert cutover forward? I'd like it before the release." }
    - { speaker: Yuri, text: "Not really. The cutover has to happen within a day of the app dashboards, so Thursday morning at the earliest. I'd rather not rush it on release day." }
    - { speaker: Ana, text: "Just so it's clear: I'm shutting the old cluster down on Thursday afternoon, so I need everything migrated no later than Thursday noon." }
    - { speaker: Yuri, text: "Understood. Worst case, if the datasource slips, I'll flag it on Tuesday and we push the shutdown back a day. Does that work?" }
    - { speaker: Ana, text: "Works for me. Just don't leave it for Wednesday night." }
    - { speaker: Priya, text: "Good. Wednesday for the dashboards, Thursday noon hard deadline. Next." }
  notes:
    - '"as of yesterday" = desde ontem / com data de ontem.'
    - '"at the earliest" = no mínimo (data); "at the latest" = no máximo.'
    - '"Does that work?" = funciona para vocês? Pergunta padrão para fechar um combinado.'
    - 'Repare que ninguém diz "until": prazos são sempre "by" e "no later than".'

listening:
  lines:
    - { speaker: Marcos, text: "Quick update on the node pool: the upgrade should be done by EOD today. Worst case, tomorrow morning. I'll ping the channel when it's through." }
    - { speaker: Ana, text: "I'm on the certificate rotation until Monday, then I'm picking up the alerting rules. The rotation has to be done no later than Sunday night, because the old certs expire on Monday." }
    - { speaker: Priya, text: "Reminder: the release is Thursday at 2 pm BRT. Anything that isn't merged by Wednesday EOD waits for the next one." }
  questions:
    - { id: M01-03-l1, type: multiple_choice, prompt: "When should the node pool upgrade be done?", options: ["By EOD today", "Until tomorrow", "Within a week", "On Monday"], answer: 0, explanation: '"the upgrade should be done by EOD today"', tags: [comp.listening, gram.by-until] }
    - { id: M01-03-l2, type: multiple_choice, prompt: "How long will Ana be on the certificate rotation?", options: ["Until Sunday night", "Until Monday", "Until Thursday", "Until EOD"], answer: 1, explanation: '"I''m on the certificate rotation until Monday" — duração, por isso until.', tags: [comp.listening, gram.by-until] }
    - { id: M01-03-l3, type: fill_blank, prompt: "The rotation must be done no later than ___ night.", accepted: ["Sunday"], explanation: '"no later than Sunday night, because the old certs expire on Monday"', tags: [comp.listening] }
    - { id: M01-03-l4, type: multiple_choice, prompt: "What happens to a PR that is not merged by Wednesday EOD?", options: ["It is merged on Thursday morning", "It is closed", "Priya merges it herself", "It waits for the next release"], answer: 3, explanation: '"Anything that isn''t merged by Wednesday EOD waits for the next one."', tags: [comp.listening, gram.by-until] }
    - { id: M01-03-l5, type: multiple_choice, prompt: '"Worst case, tomorrow morning" means…', options: ["It will definitely be tomorrow", "Tomorrow morning is the latest it should slip to", "It is already late", "He does not know"], answer: 1, explanation: "worst case = pior cenário: o mais tarde que deve acontecer.", tags: [gram.time-expressions] }
    - { id: M01-03-l6, type: multiple_choice, prompt: "When is the release?", options: ["Wednesday EOD", "Monday", "Thursday at 2 pm BRT", "Sunday night"], answer: 2, explanation: '"the release is Thursday at 2 pm BRT"', tags: [comp.listening] }

writing:
  prompt: |
    **Cenário:** você está migrando os alertas do Prometheus para o novo cluster. A parte de infra está pronta desde ontem. A parte de aplicação depende de um endpoint que o time de backend libera na terça. O time precisa de tudo migrado antes do release de quinta às 14h BRT. Você acha que consegue até quarta se o endpoint chegar; no pior caso, quinta de manhã.

    **Tarefa:** escreva seu update de standup (60–90 palavras) com uma previsão honesta: o que está pronto, o prazo com a confiança certa, a condição e o pior caso.
  constraints:
    - { label: 'Prazo com "by" (by Wednesday / by EOD / by Thursday…)', pattern: "\\bby (wednesday|thursday|tuesday|monday|friday|eod|noon|\\d|the end|end of|tomorrow|today)" }
    - { label: '"should be done by", "aiming for" ou "no later than"', pattern: "\\b(should be done by|aiming for|no later than)\\b" }
    - { label: '"worst case" ou "at the latest"', pattern: "\\b(worst case|at the latest)\\b" }
    - { label: 'Nenhum "until" como prazo' }
  rubric:
    - Previsão com a confiança certa (should / aiming / no later than)
    - by × until × within corretos
    - Condição e pior caso explícitos
    - Concisão e registro
  model: >-
    The infra alerts are migrated as of yesterday. The app alerts depend on the new backend endpoint, which
    lands on Tuesday, so that part should be done by Wednesday EOD. Worst case, if the endpoint slips, the app
    alerts land on Thursday morning, still before the 2 pm release. I'll know more by Tuesday at the latest and
    I'll flag it in the channel right away. No blockers on my side for now.
  minWords: 60
  maxWords: 90
  tags: [comp.writing, topic.daily, gram.by-until, gram.time-expressions]

speaking:
  modeA:
    prompt: Grave um update de 45 segundos sobre uma tarefa sua com prazo (real ou o do cenário). Dê o que está pronto, uma previsão com a confiança certa, a condição e o pior caso.
    maxSeconds: 45
    targetPhrases:
      - should be done by
      - I'm aiming for
      - no later than
      - at the latest
      - worst case
      - by EOD
      - within
      - on track
      - running late
      - a day late
      - push back
      - bring forward
      - miss the deadline
      - I'll know more by
      - ETA
    checklist:
      - Disse o que já está pronto?
      - Usei "by" (não "until") para o prazo?
      - Marquei a confiança (should / aiming / no later than)?
      - Dei a condição e o pior caso?

quiz:
  - { id: M01-03-q1, type: multiple_choice, prompt: "I'll have the report ready ___ Friday.", options: ["until", "by", "within", "since"], answer: 1, explanation: "Prazo (no máximo até) = by. until é duração.", tags: [gram.by-until, br.until-by] }
  - { id: M01-03-q2, type: error_correction, prompt: "I will finish the migration until Wednesday.", accepted: ["I will finish the migration by Wednesday.", "I'll finish the migration by Wednesday.", "I'll have the migration done by Wednesday.", "I will have the migration done by Wednesday.", "I will finish the migration by Wednesday", "I'll finish the migration by Wednesday"], explanation: "Terminar + prazo pede by. \"finish until\" não existe.", tags: [br.until-by, gram.by-until] }
  - { id: M01-03-q3, type: fill_blank, prompt: "I'll be on the dashboards ___ Wednesday, then I move to alerts.", accepted: ["until", "till"], explanation: "Duração (ficar nisso durante o tempo todo até quarta) = until.", tags: [gram.by-until] }
  - { id: M01-03-q4, type: multiple_choice, prompt: "Which one is the FIRMEST commitment?", options: ["I'm aiming for Wednesday.", "It should be done by Wednesday.", "It'll be done no later than Wednesday.", "Worst case, Wednesday."], answer: 2, explanation: "no later than = prazo duro. should = confiança razoável; aiming = meta com risco; worst case = contingência.", tags: [gram.time-expressions] }
  - { id: M01-03-q5, type: translate, prompt: "Vamos perder o prazo se a datasource atrasar.", accepted: ["We'll miss the deadline if the datasource slips.", "We'll miss the deadline if the datasource slips", "We will miss the deadline if the datasource slips.", "We will miss the deadline if the datasource slips", "We'll miss the deadline if the datasource is late.", "We'll miss the deadline if the datasource is late"], explanation: "Prazo se perde com miss; atrasar = slip / be late.", tags: [br.lose-the-deadline, vocab.standup] }
  - { id: M01-03-q6, type: error_correction, prompt: "I stayed until late to finish the infra part.", accepted: ["I stayed late to finish the infra part.", "I stayed late to finish the infra part", "I worked late to finish the infra part.", "I worked late to finish the infra part"], explanation: '"until late" não existe; late já carrega a ideia.', tags: [br.until-late] }
  - { id: M01-03-q7, type: multiple_choice, prompt: "I can move the cutover ___ two days of the dashboards landing.", options: ["by", "until", "within", "for"], answer: 2, explanation: "Janela de tempo relativa = within.", tags: [gram.time-expressions, gram.by-until] }
  - { id: M01-03-q8, type: reorder, prompt: "Coloque na ordem certa.", tokens: ["more", "know", "by", "Tuesday", "I'll"], answer: "I'll know more by Tuesday", explanation: "Frase-padrão para prometer a próxima informação com data.", tags: [vocab.standup, gram.by-until] }

review:
  count: 5
  preferTags: [gram.by-until, br.until-by, gram.present-perfect]

srsCards:
  - { front: "vou ter isso pronto até quarta (prazo)", back: "I'll have it done by Wednesday", tag: gram.by-until }
  - { front: "vou ficar nisso até quarta (duração)", back: "I'll be on this until Wednesday", tag: gram.by-until }
  - { front: "em até dois dias", back: "within two days", tag: gram.time-expressions }
  - { front: "deve estar pronto até quarta", back: "It should be done by Wednesday", tag: gram.time-expressions }
  - { front: "minha meta é quarta", back: "I'm aiming for Wednesday", tag: gram.time-expressions }
  - { front: "sem falta até quarta", back: "no later than Wednesday", tag: gram.time-expressions }
  - { front: "no pior caso, quinta de manhã", back: "Worst case, Thursday morning", tag: vocab.standup }
  - { front: "vou saber mais até terça", back: "I'll know more by Tuesday", tag: vocab.standup }
  - { front: "estamos dentro do prazo", back: "We're on track", tag: vocab.standup }
  - { front: "perder o prazo", back: "miss the deadline", tag: br.lose-the-deadline }
  - { front: "fiquei até tarde", back: "I stayed late", tag: br.until-late }
  - { front: "adiar / adiantar uma data", back: "push it back / bring it forward", tag: vocab.standup }

completion:
  quizMin: 0.75
  writingMin: 3
  speakingRequired: true
```

- [ ] **Step 3: Verifique**

Run: `pnpm content:build && pnpm test && pnpm typecheck`
Expected: `gerado … (3 aula(s) com conteúdo)`; PASS (211 + 6 = 217). Atenção ao detector no modelo de escrita: "I'm on the certificate rotation until Monday" (escuta) não é escrita; o modelo usa só "by". Se `crossValidate` acusar `meaning` repetido com o glossário (`ETA`, `EOD` existem em `daily.yaml` com significados próprios), ajuste o `meaning` da aula para um texto distinto (ex.: "previsão de quando algo fica pronto" × glossário "previsão de quando algo fica pronto ou chega" já diferem) e relate.

- [ ] **Step 4: Commit**

```bash
git add content/modules/M01/lessons/M01-03.yaml tests/lessons-m01.test.ts
git commit -m "content(M01): aula M01-03 — tempo e prazos (by, until, within, ETA, EOD)"
```

---

### Task 8: Aula M01-04 — Bloqueios e dependências

**Files:**
- Create: `content/modules/M01/lessons/M01-04.yaml`
- Modify: `tests/lessons-m01.test.ts` (1 linha)

**Interfaces:**
- Consumes: `checkLesson` (T6).
- Produces: aula M01-04 jogável.

- [ ] **Step 1: Teste que falha** — em `tests/lessons-m01.test.ts`, depois da linha da M01-03:

```ts
  checkLesson("M01-04", { vocabMin: 12, dialogueMin: 6, listening: 6, quiz: 8, cardsMin: 10, maxSeconds: 45, prerequisites: ["M01-02"] });
```

Run: `pnpm test tests/lessons-m01.test.ts` → FAIL (`M01-04` undefined).

- [ ] **Step 2: Crie `content/modules/M01/lessons/M01-04.yaml`** exatamente com:

```yaml
id: M01-04
module: M01
order: 4
title: "Bloqueios e dependências: blocked on, waiting on, depends on"
objective: >-
  Ao final, você consegue nomear um bloqueio em uma frase (o que trava, desde quando, o que já fez), separar
  "estou travado" de "estou esperando", pedir ajuda em uma linha sem soar despreparado nem rude, e desbloquear
  os outros quando o bloqueio é você.
durationMin: 40
competencies: [SPK, VOC, LIS, CNF]
tags: [topic.daily, topic.help, vocab.standup, br.waiting-no-prep, br.depend-of, br.explain-me, br.doubt, gram.modals-polite, gram.prepositions-infra, comp.speaking]
prerequisites: [M01-02]

context:
  scenario: |
    Terça-feira, daily das 10h BRT. Você está migrando o `checkout-worker` para o novo cluster e travou em três pontos diferentes, de três naturezas diferentes:

    1. O pipeline falha porque a *service account* do novo cluster não tem permissão de ler o bucket de artefatos. Você abriu um ticket para o time de segurança ontem de manhã e ninguém respondeu.
    2. O Marcos prometeu o Helm chart base na sexta e ainda não entregou; sem ele você não consegue testar o rollout.
    3. Você não entendeu como o *secret* do banco é injetado no cluster novo; leu o runbook duas vezes e continua sem entender.

    O primeiro é um **bloqueio** por terceiros, o segundo é uma **dependência** de um colega, o terceiro é uma **dúvida** sua. Cada um pede uma frase diferente. E a Ana está esperando **você** revisar o PR dela desde ontem.
  roles: [Priya (scrum master), Ana (SRE), Marcos (platform engineer)]

vocabulary:
  - term: I'm blocked on X
    meaning: X me impede de avançar (o problema, não a pessoa)
    example: I'm blocked on the service account permissions for the artifacts bucket.
    translation: Estou travado nas permissões da service account para o bucket de artefatos.
    note: '"blocked on" + o problema. "blocked by Ana" soa acusatório; prefira "blocked on the review".'
  - term: I'm waiting on X (from Y)
    meaning: dependo de algo que alguém ainda não entregou
    example: I'm waiting on the base Helm chart from Marcos.
    translation: Estou esperando o chart base do Marcos.
    note: '"waiting on/for". Sem preposição é erro. Nomeia a coisa, depois a pessoa.'
  - term: X depends on Y
    meaning: X só acontece depois de Y
    example: The rollout test depends on the base chart.
    translation: O teste de rollout depende do chart base.
    note: '"depend on", nunca "depend of".'
  - term: I need X from Y
    meaning: pedido direto e específico
    example: I need read access to the artifacts bucket from the security team.
    translation: Preciso de acesso de leitura ao bucket de artefatos, do time de segurança.
    note: Diga o quê e de quem; evita "I need help".
  - term: I've asked in #channel / I opened a ticket
    meaning: mostrar que já agiu
    example: I opened a ticket with security yesterday morning; no reply yet.
    translation: Abri um ticket com segurança ontem de manhã; sem resposta ainda.
    note: Bloqueio sem ação tomada parece desculpa.
  - term: Who owns X?
    meaning: quem é responsável por X
    example: Who owns the artifacts bucket policy?
    translation: Quem cuida da policy do bucket de artefatos?
    note: '"own" = ser dono/responsável. Pergunta que destrava rápido.'
  - term: Could you point me to…?
    meaning: pode me indicar onde…
    example: Could you point me to where the DB secret gets injected?
    translation: Pode me indicar onde o secret do banco é injetado?
    note: Pedido educado por informação, não por trabalho.
  - term: Could someone walk me through X?
    meaning: alguém pode me explicar X passo a passo
    example: Could someone walk me through the secret injection after the call?
    translation: Alguém pode me explicar a injeção de secrets depois da call?
    note: Substitui "explain me". "after the call" protege o daily.
  - term: I'm not sure I follow how X works
    meaning: admitir que não entendeu, sem drama
    example: I'm not sure I follow how the secret gets into the pod.
    translation: Não entendi bem como o secret chega no pod.
    note: Registro neutro; melhor que "I don't understand anything".
  - term: I have a question about X
    meaning: tenho uma dúvida (pergunta) sobre X
    example: I have a question about the secret injection.
    translation: Tenho uma dúvida sobre a injeção de secrets.
    note: Nunca "doubt" nesse sentido.
  - term: Can you unblock me on X?
    meaning: pode me destravar em X
    example: Ana, can you unblock me on the bucket policy? You have admin there.
    translation: Ana, pode me destravar na policy do bucket? Você tem admin lá.
    note: Direto e específico; para colegas próximos.
  - term: It's on me
    meaning: a responsabilidade (ou o atraso) é minha
    example: The review is on me; I'll do it right after this.
    translation: A revisão está comigo; faço logo depois disso.
    note: Assume o bloqueio que você causa nos outros.
  - term: I'll get to it right after this
    meaning: faço isso logo depois (da call)
    example: I'll get to your PR right after this.
    translation: Vou pegar seu PR logo depois disso.
    note: '"get to" = chegar em (uma tarefa da fila).'
  - term: This is blocking Ana's release
    meaning: nomear o impacto do bloqueio
    example: The bucket policy is blocking the checkout release, so it's urgent.
    translation: A policy do bucket está bloqueando o release do checkout, então é urgente.
    note: Impacto + palavra "urgent" só quando é.
  - term: If I don't hear back by X, I'll…
    meaning: prazo para a espera + plano B
    example: If I don't hear back from security by noon, I'll ask Priya to escalate.
    translation: Se segurança não responder até o meio-dia, peço para a Priya escalar.
    note: Espera com data e plano B; nunca "estou esperando" sem fim.

grammar:
  title: "Preposições de dependência e o pedido em uma frase"
  explanation: |
    **Três situações, três frases:**

    | Situação | Frase | Preposição |
    |---|---|---|
    | Algo me impede | I'm **blocked on** the bucket permissions. | on |
    | Alguém ainda não entregou | I'm **waiting on/for** the chart **from** Marcos. | on / for + from |
    | Uma coisa só acontece depois de outra | The test **depends on** the chart. | on |

    *Blocked* é o problema; *waiting* é a entrega; *depends* é a ordem. As três usam **on**. "Depend of" e "waiting the" são calques.

    **O pedido em uma frase:** contexto curto → o que já tentei → pedido específico.

    > I'm blocked on the bucket permissions; I opened a ticket yesterday with no reply. **Could someone from security take a look after the call?**

    **Modais de polidez, do mais leve ao mais direto:** *Could you point me to…?* · *Would you mind…?* · *Can you unblock me on…?* · *I need X from Y by Z.* Quanto mais urgente, mais direto; quanto mais direto, mais específico.

    **Dúvida ≠ bloqueio:** "I'm not sure I follow how X works" pede explicação; "I'm blocked on X" pede ação. Misturar os dois confunde quem pode ajudar.
  examples:
    - { en: "I'm blocked on the bucket permissions.", pt: "Estou travado nas permissões do bucket." }
    - { en: "I'm waiting on the chart from Marcos.", pt: "Estou esperando o chart do Marcos." }
    - { en: "The rollout test depends on the base chart.", pt: "O teste de rollout depende do chart base." }
    - { en: "Could someone walk me through the secret injection?", pt: "Alguém pode me explicar a injeção de secrets?" }
    - { en: "I need read access from the security team by noon.", pt: "Preciso de acesso de leitura do time de segurança até o meio-dia." }

examples:
  - en: I'm blocked on the service account permissions for the artifacts bucket. I opened a ticket with security yesterday morning; no reply yet.
    pt: Estou travado nas permissões da service account para o bucket de artefatos. Abri um ticket com segurança ontem de manhã; sem resposta ainda.
    context: Bloqueio nomeado com o que já foi feito.
  - en: I'm waiting on the base Helm chart from Marcos; the rollout test depends on it.
    pt: Estou esperando o chart base do Marcos; o teste de rollout depende dele.
    context: Dependência de colega, sem acusar, com o impacto.
  - en: I'm not sure I follow how the DB secret gets injected in the new cluster. Could someone walk me through it after the call?
    pt: Não entendi bem como o secret do banco é injetado no cluster novo. Alguém pode me explicar depois da call?
    context: Dúvida admitida + pedido educado fora do daily.
  - en: Who owns the bucket policy? If it's security, I'd like to escalate.
    pt: Quem cuida da policy do bucket? Se for segurança, quero escalar.
    context: Descobrir o dono é o caminho mais curto para destravar.
  - en: If I don't hear back by noon, I'll ask Priya to escalate.
    pt: Se não responderem até o meio-dia, peço para a Priya escalar.
    context: Espera com prazo e plano B.
  - en: Ana, your PR is on me. I'll get to it right after this.
    pt: Ana, seu PR está comigo. Pego logo depois disso.
    context: Você é o bloqueio de alguém; assume e dá prazo.
  - en: Marcos, any ETA on the base chart? Not pushing, just planning around it.
    pt: Marcos, tem previsão do chart base? Não estou cobrando, só planejando em cima disso.
    context: Cobrar um colega sem soar rude.

variations:
  - idea: '"pedir ajuda com o secret"'
    items:
      - { register: Neutro (daily), text: "Could someone walk me through the secret injection after the call?", adequate: true }
      - { register: Direto (colega próximo), text: "Marcos, can you show me the secret injection real quick after this?", adequate: true }
      - { register: Formal (canal amplo), text: "Would anyone with context on secret injection in the new cluster be able to spare 15 minutes today?", adequate: true }
      - { register: Inadequada, text: "Can you explain me the secret?", adequate: false, note: '"explain me" é calque; e "the secret" é vago.' }
      - { register: Inadequada, text: "I don't understand anything about secrets, sorry.", adequate: false, note: Sem foco; ninguém sabe por onde começar a ajudar. }
  - idea: '"estou esperando o chart do Marcos"'
    items:
      - { register: Neutro, text: "I'm waiting on the base chart from Marcos.", adequate: true }
      - { register: Com impacto, text: "The rollout test is waiting on Marcos's chart, so it slips a day.", adequate: true }
      - { register: Inadequada, text: "Marcos didn't deliver the chart, so I can't work.", adequate: false, note: Acusa a pessoa em público e não diz o que você pode fazer enquanto isso. }
      - { register: Inadequada, text: "I'm waiting the chart.", adequate: false, note: Sem preposição. }

brErrors:
  - { wrong: "I'm waiting the chart from Marcos.", right: "I'm waiting on the chart from Marcos.", why: "wait exige preposição", tag: br.waiting-no-prep }
  - { wrong: "The test depends of the chart.", right: "The test depends on the chart.", why: "depend on", tag: br.depend-of }
  - { wrong: "Can you explain me the secret injection?", right: "Can you walk me through the secret injection?", why: "explain não aceita objeto indireto direto", tag: br.explain-me }
  - { wrong: "I have a doubt about the secret.", right: "I have a question about the secret.", why: "doubt = desconfiança", tag: br.doubt }
  - { wrong: "I'm blocked by Marcos.", right: "I'm blocked on the base chart (from Marcos).", why: "blocked on + o problema; nomear a pessoa como bloqueio soa acusatório", tag: vocab.standup }
  - { wrong: "I need help.", right: "I need read access to the bucket from security.", why: "pedido sem objeto não destrava nada", tag: topic.help }
  - { wrong: "You can give me access?", right: "Could you give me access?", why: "pergunta pede inversão + modal de polidez", tag: gram.modals-polite }
  - { wrong: "I'm in the new cluster now.", right: "I'm on the new cluster now.", why: "cluster/servidor/ambiente: on", tag: gram.prepositions-infra }
  - { wrong: "Sorry to bother, but maybe if it's possible could you perhaps…", right: "Could you take a look at the bucket policy after the call?", why: "excesso de suavizadores soa inseguro; um modal basta", tag: gram.modals-polite }

dialogue:
  title: "Daily de terça, 10:00 BRT. Três travas e um PR esperando você."
  lines:
    - { speaker: Priya, text: "Yuri, how's the checkout-worker migration?" }
    - { speaker: Yuri, text: "Slower than I'd like. I'm blocked on the service account permissions for the artifacts bucket; I opened a ticket with security yesterday morning and haven't heard back. Separately, I'm waiting on the base Helm chart from Marcos, because the rollout test depends on it. And I have a question about how the DB secret gets injected, but that can wait until after the call.", note: "Bloqueio / dependência / dúvida, cada um com a frase certa." }
    - { speaker: Priya, text: "Who owns the bucket policy?" }
    - { speaker: Ana, text: "Security, but I have admin on that bucket. I can add the read permission right after this. Yuri, can you unblock me on my PR? It's been sitting since yesterday." }
    - { speaker: Yuri, text: "Yes, that's on me. I'll get to it right after this, before the bucket stuff." }
    - { speaker: Marcos, text: "Chart's almost done. I'll have it in the repo by EOD; the values file is what's left." }
    - { speaker: Yuri, text: "Great. If I get the permissions today, I can test the rollout tomorrow morning. Marcos, could you walk me through the secret injection when you push the chart?" }
    - { speaker: Marcos, text: "Sure, I'll ping you." }
    - { speaker: Priya, text: "Good. Ana unblocks the bucket, Yuri reviews Ana's PR, Marcos pushes the chart by EOD. Next." }
  notes:
    - '"Slower than I''d like" abre com honestidade sem drama.'
    - '"It''s been sitting since yesterday" = está parado desde ontem (cobrança leve).'
    - 'Priya fecha com quem faz o quê: é assim que um bloqueio vira ação.'

listening:
  lines:
    - { speaker: Ana, text: "I'm blocked on the firewall rule for the new load balancer. I asked in the network channel on Friday and again yesterday. If I don't hear back by noon, I'll escalate to their lead." }
    - { speaker: Marcos, text: "The node pool upgrade depends on the new AMI from the platform team; they said Thursday. Not blocked yet, just planning around it. I'm waiting for their confirmation." }
    - { speaker: Priya, text: "Yuri, the release notes are waiting on you; I need the checkout section by tomorrow. Also, who owns the Grafana folder for the old dashboards? We should archive it." }
  questions:
    - { id: M01-04-l1, type: multiple_choice, prompt: "What is Ana blocked on?", options: ["A firewall rule", "The new AMI", "The release notes", "The Grafana folder"], answer: 0, explanation: '"I''m blocked on the firewall rule for the new load balancer."', tags: [comp.listening] }
    - { id: M01-04-l2, type: multiple_choice, prompt: "What will Ana do if nobody replies by noon?", options: ["Open a ticket", "Wait until Friday", "Ask Marcos", "Escalate to the network lead"], answer: 3, explanation: '"If I don''t hear back by noon, I''ll escalate to their lead."', tags: [comp.listening, topic.help] }
    - { id: M01-04-l3, type: fill_blank, prompt: "The node pool upgrade depends ___ the new AMI.", accepted: ["on"], explanation: "depend on.", tags: [br.depend-of, comp.listening] }
    - { id: M01-04-l4, type: multiple_choice, prompt: "Is Marcos blocked?", options: ["Yes, since Friday", "Yes, on the AMI", "Not yet; he is planning around it", "He does not say"], answer: 2, explanation: '"Not blocked yet, just planning around it."', tags: [comp.listening, vocab.standup] }
    - { id: M01-04-l5, type: multiple_choice, prompt: "What is waiting on Yuri?", options: ["The firewall rule", "The release notes", "The AMI", "The node pool"], answer: 1, explanation: '"the release notes are waiting on you"', tags: [comp.listening, br.waiting-no-prep] }
    - { id: M01-04-l6, type: multiple_choice, prompt: '"Who owns the Grafana folder?" — Priya wants to know…', options: ["who created it", "who uses it most", "who deleted it", "who is responsible for it"], answer: 3, explanation: "own = ser responsável por.", tags: [vocab.standup] }

writing:
  prompt: |
    **Cenário:** você está migrando um cron de faturamento para o novo cluster. Travou porque a imagem não sobe: falta um segredo que só o time de pagamentos pode criar. Você mandou mensagem no canal deles ontem à tarde, sem resposta. Além disso, o teste depende de um dump do banco que a Ana ficou de gerar. Você não entendeu como o *CronJob* recebe o fuso horário no cluster novo.

    **Tarefa:** escreva seu update de standup (60–90 palavras) separando bloqueio, dependência e dúvida, com o que você já fez e um pedido específico em uma frase.
  constraints:
    - { label: '"blocked on"', pattern: "\\bblocked on\\b" }
    - { label: '"waiting on/for" ou "depends on"', pattern: "\\b(waiting (on|for)|depends on)\\b" }
    - { label: 'Um pedido com could / can / would', pattern: "\\b(could|can|would) (you|someone|anyone)\\b" }
    - { label: "O que você já fez (asked / opened / pinged)", pattern: "\\b(asked|opened|pinged|messaged|posted)\\b" }
  rubric:
    - Bloqueio, dependência e dúvida separados
    - Preposições (blocked on, waiting on, depends on)
    - Pedido específico em uma frase
    - Concisão e registro
  model: >-
    I'm blocked on the billing cron: the image won't start because it needs a secret that only the payments
    team can create. I messaged their channel yesterday afternoon and haven't heard back. The end-to-end test
    also depends on the database dump Ana is generating, so I'm waiting on that. And I have a question about
    how the CronJob gets its time zone in the new cluster. Could someone from payments create the secret today?
  minWords: 60
  maxWords: 90
  tags: [comp.writing, topic.daily, topic.help]

speaking:
  modeA:
    prompt: Grave um update de 45 segundos sobre um bloqueio seu (real ou o do cenário). Nomeie o que trava, o que já fez e faça um pedido específico em uma frase.
    maxSeconds: 45
    targetPhrases:
      - I'm blocked on
      - I'm waiting on
      - I'm waiting for
      - depends on
      - I need
      - I opened a ticket
      - I've asked
      - who owns
      - could you point me to
      - walk me through
      - I'm not sure I follow
      - I have a question about
      - unblock me
      - it's on me
      - right after this
      - if I don't hear back by
    checklist:
      - Separei bloqueio, dependência e dúvida?
      - Disse o que já fiz antes de pedir?
      - O pedido tem objeto e destinatário?
      - Usei on (blocked on, waiting on, depends on)?

quiz:
  - { id: M01-04-q1, type: fill_blank, prompt: "I'm waiting ___ the chart from Marcos.", accepted: ["on", "for"], explanation: "wait on / wait for. Sem preposição é erro.", tags: [br.waiting-no-prep] }
  - { id: M01-04-q2, type: error_correction, prompt: "The rollout test depends of the base chart.", accepted: ["The rollout test depends on the base chart.", "The rollout test depends on the base chart"], explanation: "depend on.", tags: [br.depend-of] }
  - { id: M01-04-q3, type: multiple_choice, prompt: "Which sentence names the problem, not the person?", options: ["I'm blocked by Ana.", "Ana is blocking me.", "I'm blocked on the bucket policy.", "Ana didn't do her part."], answer: 2, explanation: "blocked on + o problema. Nomear a pessoa como bloqueio soa acusatório em público.", tags: [vocab.standup, topic.daily] }
  - { id: M01-04-q4, type: error_correction, prompt: "Can you explain me the secret injection?", accepted: ["Can you walk me through the secret injection?", "Can you walk me through the secret injection", "Can you explain the secret injection to me?", "Can you explain the secret injection to me", "Could you walk me through the secret injection?", "Could you explain the secret injection to me?"], explanation: "explain + coisa + to me, ou walk me through.", tags: [br.explain-me, gram.modals-polite] }
  - { id: M01-04-q5, type: multiple_choice, prompt: "Which request is the most useful?", options: ["I need help.", "Can someone help me with stuff?", "I need read access to the artifacts bucket from security.", "Sorry, maybe someone could maybe help?"], answer: 2, explanation: "Objeto + destinatário. As outras não dizem o que nem de quem.", tags: [topic.help, gram.modals-polite] }
  - { id: M01-04-q6, type: translate, prompt: "Não entendi bem como o secret chega no pod.", accepted: ["I'm not sure I follow how the secret gets into the pod.", "I'm not sure I follow how the secret gets into the pod", "I'm not sure how the secret gets into the pod.", "I'm not sure how the secret gets into the pod", "I don't quite follow how the secret gets into the pod.", "I'm not sure I understand how the secret gets into the pod."], explanation: '"I''m not sure I follow" = não entendi bem, sem drama.', tags: [topic.help, vocab.standup] }
  - { id: M01-04-q7, type: reorder, prompt: "Coloque na ordem certa.", tokens: ["back", "noon", "by", "hear", "don't", "if", "I"], answer: "if I don't hear back by noon", explanation: "Espera com prazo: if I don't hear back by + hora, I'll + plano B.", tags: [vocab.standup, gram.by-until] }
  - { id: M01-04-q8, type: match, prompt: "Associe a situação à frase.", pairs: [{ left: "algo me impede", right: "I'm blocked on the permissions." }, { left: "alguém ainda não entregou", right: "I'm waiting on the chart from Marcos." }, { left: "uma coisa só acontece depois da outra", right: "The test depends on the chart." }, { left: "não entendi", right: "I'm not sure I follow how it works." }], explanation: "Quatro situações, quatro frases. As três primeiras usam on.", tags: [vocab.standup, topic.help] }

review:
  count: 5
  preferTags: [br.waiting-no-prep, br.doubt, gram.modals-polite]

srsCards:
  - { front: "estou travado nas permissões do bucket", back: "I'm blocked on the bucket permissions", tag: vocab.standup }
  - { front: "estou esperando o chart do Marcos", back: "I'm waiting on the chart from Marcos", tag: br.waiting-no-prep }
  - { front: "o teste depende do chart", back: "The test depends on the chart", tag: br.depend-of }
  - { front: "preciso de acesso de leitura, do time de segurança", back: "I need read access from the security team", tag: topic.help }
  - { front: "abri um ticket ontem; sem resposta ainda", back: "I opened a ticket yesterday; no reply yet", tag: vocab.standup }
  - { front: "quem cuida da policy do bucket?", back: "Who owns the bucket policy?", tag: topic.help }
  - { front: "pode me indicar onde…?", back: "Could you point me to where…?", tag: gram.modals-polite }
  - { front: "alguém pode me explicar passo a passo depois da call?", back: "Could someone walk me through it after the call?", tag: br.explain-me }
  - { front: "não entendi bem como isso funciona", back: "I'm not sure I follow how it works", tag: topic.help }
  - { front: "pode me destravar nisso?", back: "Can you unblock me on this?", tag: topic.help }
  - { front: "isso está comigo; faço logo depois", back: "That's on me; I'll get to it right after this", tag: vocab.standup }
  - { front: "se não responderem até o meio-dia, eu escalo", back: "If I don't hear back by noon, I'll escalate", tag: vocab.standup }

completion:
  quizMin: 0.75
  writingMin: 3
  speakingRequired: true
```

- [ ] **Step 3: Verifique**

Run: `pnpm content:build && pnpm test && pnpm typecheck`
Expected: `(4 aula(s) com conteúdo)`; PASS (217 + 6 = 223). Cuidado com o `crossValidate` de significados: `meaning` de "I'm blocked on X" aqui ("X me impede de avançar (o problema, não a pessoa)") difere do da M01-02 ("algo impede você de avançar") e do glossário ("travado por causa de algo específico"); se colidir, ajuste o texto da aula nova.

- [ ] **Step 4: Commit**

```bash
git add content/modules/M01/lessons/M01-04.yaml tests/lessons-m01.test.ts
git commit -m "content(M01): aula M01-04 — bloqueios e dependências"
```

---

### Task 9: Aula M01-05 — Standup completo (simulação sem LLM)

**Files:**
- Create: `content/modules/M01/lessons/M01-05.yaml`
- Modify: `tests/lessons-m01.test.ts` (1 linha)

**Interfaces:**
- Consumes: `checkLesson` (T6).
- Produces: aula M01-05 jogável (integra o módulo: diálogo com 3 personas + Yuri, escuta longa com 8 perguntas, fala de 60 s, escrita de follow-up no Slack de 80–120 palavras).

- [ ] **Step 1: Teste que falha** — em `tests/lessons-m01.test.ts`, depois da linha da M01-04:

```ts
  checkLesson("M01-05", { vocabMin: 12, dialogueMin: 12, listening: 8, quiz: 8, cardsMin: 10, maxSeconds: 60, prerequisites: ["M01-03", "M01-04"] });
```

Run: `pnpm test tests/lessons-m01.test.ts` → FAIL (`M01-05` undefined).

- [ ] **Step 2: Crie `content/modules/M01/lessons/M01-05.yaml`** exatamente com:

```yaml
id: M01-05
module: M01
order: 5
title: "Standup completo com 3 personas + parking lot"
objective: >-
  Ao final, você conduz uma rodada inteira de standup: dá o seu update com as três partes e o prazo certo,
  responde a follow-ups sem se perder, joga o que não cabe no daily para o parking lot e escreve o follow-up
  no Slack depois da call, com tom e formato de time distribuído.
durationMin: 45
competencies: [SPK, LIS, VOC, CNF]
tags: [topic.daily, topic.async, vocab.standup, vocab.slack, gram.present-perfect, gram.by-until, gram.since-for, br.since-present, br.until-by, br.waiting-no-prep, comp.speaking]
prerequisites: [M01-03, M01-04]

context:
  scenario: |
    Quinta-feira, daily das 10h BRT, 15 minutos, quatro pessoas: Priya (scrum master), Ana (SRE), Marcos (platform) e você. Release hoje às 14h BRT.

    Sua semana: na segunda você começou a migração do `checkout-worker`; terça e quarta destravou permissões e testou o rollout com o chart do Marcos; ontem à noite o rollout em staging passou. Hoje você quer fazer o deploy em produção **antes** do release, mas o job de smoke test está intermitente (passou 3 de 5 vezes) e você ainda não sabe por quê. A Ana precisa do cluster antigo desligado até amanhã. O Marcos vai perguntar sobre o smoke test; a Priya vai querer uma decisão: deploy antes ou depois do release.

    Duas coisas não cabem no daily e vão para o *parking lot*: a discussão sobre a causa do smoke test intermitente e a dúvida sobre quem apaga os dashboards antigos. Depois da call, você escreve o follow-up no canal `#platform` com o que ficou combinado.
  roles: [Priya (scrum master), Ana (SRE), Marcos (platform engineer)]

vocabulary:
  - term: Let's go around
    meaning: vamos passar por todo mundo (abre a rodada)
    example: Morning all. Let's go around — Ana, you're up.
    translation: Bom dia. Vamos passar por todos. Ana, é a sua vez.
    note: Quem conduz diz; você responde ao "you're up".
  - term: Since Monday I've been…
    meaning: resumo da semana (começou e continua)
    example: Since Monday I've been on the checkout-worker migration.
    translation: Desde segunda estou na migração do checkout-worker.
    note: since + present perfect; nunca "I'm on it since Monday".
  - term: As of last night, X is…
    meaning: estado desde ontem à noite (com data)
    example: As of last night, the staging rollout is green.
    translation: Desde ontem à noite, o rollout de staging está verde.
    note: '"as of" marca o momento da informação.'
  - term: The plan is to…
    meaning: o plano é… (decisão que depende de confirmação)
    example: The plan is to deploy to prod before the 2 pm release.
    translation: O plano é fazer o deploy em produção antes do release das 14h.
    note: Mais suave que "I will"; convida objeção.
  - term: flaky / intermittent
    meaning: instável, falha de forma intermitente
    example: The smoke test is flaky: it passed three out of five runs.
    translation: O smoke test está instável: passou em três de cinco execuções.
    note: '"three out of five" = 3 de 5.'
  - term: I haven't figured out why yet
    meaning: ainda não descobri o motivo
    example: I haven't figured out why yet.
    translation: Ainda não descobri por quê.
    note: '"figure out" = descobrir/entender; yet no fim.'
  - term: Let's take that offline / to the parking lot
    meaning: vamos tratar disso fora do daily
    example: Let's take the smoke test discussion offline; I'll grab you after the call, Marcos.
    translation: Vamos discutir o smoke test depois; te chamo depois da call, Marcos.
    note: '"take it offline" = fora da reunião (não tem a ver com internet).'
  - term: Can I get a decision on X?
    meaning: preciso que alguém decida X
    example: Priya, can I get a decision on deploying before or after the release?
    translation: Priya, preciso de uma decisão: deploy antes ou depois do release?
    note: Pede decisão sem tomar sozinho o que não é seu.
  - term: My recommendation is…
    meaning: minha recomendação é…
    example: My recommendation is to deploy after the release, given the flaky smoke test.
    translation: Minha recomendação é fazer o deploy depois do release, dado o smoke test instável.
    note: Dê a opinião antes de pedir a decisão.
  - term: given that…
    meaning: considerando que…
    example: Given that the release is at 2 pm, I'd rather not deploy right before it.
    translation: Considerando que o release é às 14h, prefiro não fazer o deploy logo antes.
    note: Conecta o motivo à decisão.
  - term: I'd rather (not)…
    meaning: eu preferiria (não)…
    example: I'd rather not rush it.
    translation: Prefiro não fazer isso correndo.
    note: '"I''d rather" + verbo sem to.'
  - term: Does that work for everyone?
    meaning: funciona para todo mundo? (fecha um combinado)
    example: Deploy tomorrow at 9, shutdown after that. Does that work for everyone?
    translation: Deploy amanhã às 9, desligamento depois. Funciona para todos?
    note: Convida objeção uma última vez antes de fechar.
  - term: To recap / Quick recap:
    meaning: resumindo o que ficou combinado
    example: "Quick recap: prod deploy tomorrow 9 am BRT, Ana shuts the old cluster down after."
    translation: "Resumindo: deploy em prod amanhã 9h BRT, Ana desliga o cluster antigo depois."
    note: Abre o follow-up no Slack.
  - term: Action items:
    meaning: tarefas combinadas, com dono
    example: "Action items: Yuri — deploy; Marcos — look into the smoke test; Ana — shutdown."
    translation: "Ações: Yuri, deploy; Marcos, investigar o smoke test; Ana, desligamento."
    note: Nome + travessão + ação.
  - term: Thread below for X
    meaning: detalhe na thread abaixo
    example: Thread below for the smoke test details.
    translation: Detalhes do smoke test na thread abaixo.
    note: Mantém o canal limpo; padrão de Slack.
  - term: Heads up, timing:
    meaning: aviso sobre horário/fuso
    example: "Heads up, timing: 9 am BRT is 1 pm for Marcos, so he'll join late."
    translation: "Aviso sobre horário: 9h BRT são 13h para o Marcos, então ele entra depois."
    note: Times distribuídos vivem disso.

grammar:
  title: "Juntando tudo: tempos verbais ao longo da semana e a linguagem de decisão"
  explanation: |
    Um update de fim de semana de trabalho percorre quatro tempos:

    | Momento | Forma | Exemplo |
    |---|---|---|
    | A semana até agora | present perfect (continuous) + *since* | **Since Monday I've been** on the migration. |
    | Um fato fechado | past simple | **Yesterday** the staging rollout **passed**. |
    | O estado agora | present perfect / present + *as of* | **As of last night**, staging **is** green. |
    | O plano | *the plan is to* / *going to* / *by* | **The plan is to** deploy **by** noon. |

    **Linguagem de decisão** (quando o que você faz afeta os outros):

    1. Contexto em uma frase: *The smoke test is flaky, three out of five.*
    2. Opinião: *My recommendation is to deploy after the release.*
    3. Motivo: *Given that the release is at 2 pm, I'd rather not rush it.*
    4. Pedido de decisão: *Priya, can I get a decision on that?*
    5. Fechamento: *Does that work for everyone?*

    **Parking lot:** o que exige discussão sai do daily com uma frase (*let's take that offline*) e uma pessoa responsável por retomar.

    **Follow-up no Slack:** *Quick recap* → decisões → *Action items* com dono → *Heads up, timing* se houver fuso. Curto; detalhe vai para a thread.
  examples:
    - { en: "Since Monday I've been on the migration.", pt: "Desde segunda estou na migração." }
    - { en: "Yesterday the staging rollout passed.", pt: "Ontem o rollout de staging passou." }
    - { en: "As of last night, staging is green.", pt: "Desde ontem à noite, staging está verde." }
    - { en: "The plan is to deploy by noon.", pt: "O plano é fazer o deploy até o meio-dia." }
    - { en: "Given that the release is at 2 pm, I'd rather not rush it.", pt: "Considerando que o release é às 14h, prefiro não correr." }

examples:
  - en: Since Monday I've been on the checkout-worker migration. As of last night, the staging rollout is green.
    pt: Desde segunda estou na migração do checkout-worker. Desde ontem à noite, o rollout de staging está verde.
    context: Abertura do update de quinta: semana + estado atual.
  - en: The smoke test is flaky, three out of five runs, and I haven't figured out why yet.
    pt: O smoke test está instável, três de cinco execuções, e ainda não descobri por quê.
    context: Problema com número e honestidade.
  - en: My recommendation is to deploy to prod tomorrow morning rather than right before the release. Priya, can I get a decision on that?
    pt: Minha recomendação é fazer o deploy em prod amanhã de manhã, não logo antes do release. Priya, preciso de uma decisão.
    context: Opinião + pedido de decisão, sem decidir sozinho.
  - en: Marcos, let's take the smoke test discussion offline; I'll grab you after the call.
    pt: Marcos, vamos discutir o smoke test depois; te chamo depois da call.
    context: Parking lot com dono e momento.
  - en: Ana, that means the old cluster shutdown moves to tomorrow afternoon. Does that work for you?
    pt: Ana, isso significa que o desligamento do cluster antigo passa para amanhã à tarde. Funciona para você?
    context: Nomear o impacto na outra pessoa e perguntar.
  - en: "Quick recap from standup: prod deploy of checkout-worker tomorrow at 9 am BRT; old cluster shutdown after that."
    pt: "Resumo do standup: deploy do checkout-worker em prod amanhã às 9h BRT; desligamento do cluster antigo depois."
    context: Primeira linha do follow-up no Slack.
  - en: "Action items: Yuri — prod deploy 9 am; Marcos — look into the flaky smoke test (thread below); Ana — shutdown after the deploy."
    pt: "Ações: Yuri, deploy 9h; Marcos, investigar o smoke test instável (thread abaixo); Ana, desligamento depois do deploy."
    context: Ações com dono; detalhe na thread.

variations:
  - idea: '"proponho adiar o deploy"'
    items:
      - { register: Neutro (daily), text: "My recommendation is to deploy tomorrow morning instead. Can I get a decision?", adequate: true }
      - { register: Direto (time próximo), text: "I'd push the deploy to tomorrow 9 am. Objections?", adequate: true }
      - { register: Formal (canal amplo), text: "Given the flaky smoke test, I propose moving the production deploy to tomorrow 9 am BRT.", adequate: true }
      - { register: Inadequada, text: "I don't know, maybe we deploy, maybe not, what do you think?", adequate: false, note: Sem recomendação; empurra a decisão sem dado. }
      - { register: Inadequada, text: "I will not deploy today, period.", adequate: false, note: Decide sozinho algo que afeta o release de todos. }
  - idea: '"follow-up no Slack"'
    items:
      - { register: Neutro, text: "Quick recap: deploy tomorrow 9 am BRT. Action items: Yuri — deploy; Ana — shutdown after. Thread below for the smoke test.", adequate: true }
      - { register: Informal, text: "Recap 👇 deploy tomorrow 9 BRT, Ana shuts old cluster after. Smoke test stuff in thread.", adequate: true, note: Com time próximo e canal informal. }
      - { register: Inadequada, text: "Hello everyone, hope you are well. As discussed in our meeting today, we have decided…", adequate: false, note: '"no hello" e sem preâmbulo; o Slack pede o resumo na primeira linha.' }
      - { register: Inadequada, text: "deploy tomorrow", adequate: false, note: Sem hora, sem fuso, sem dono. }

brErrors:
  - { wrong: "I'm on the migration since Monday.", right: "I've been on the migration since Monday.", why: "since pede present perfect", tag: br.since-present }
  - { wrong: "I will deploy until noon.", right: "I'll deploy by noon.", why: "prazo = by", tag: br.until-by }
  - { wrong: "I'm waiting Ana's confirmation.", right: "I'm waiting on Ana's confirmation.", why: "wait exige preposição", tag: br.waiting-no-prep }
  - { wrong: "The smoke test is giving error sometimes.", right: "The smoke test is flaky / fails intermittently.", why: "calque de 'dando erro'", tag: br.giving-error }
  - { wrong: "Let's discuss about the smoke test after.", right: "Let's discuss the smoke test after the call.", why: "discuss é transitivo direto", tag: br.discuss-about }
  - { wrong: "I have a doubt if we should deploy today.", right: "I'm not sure we should deploy today.", why: "doubt = desconfiança", tag: br.doubt }
  - { wrong: "Actually the rollout is green. (= atualmente)", right: "As of last night, the rollout is green.", why: "actually = na verdade", tag: br.actually }
  - { wrong: "It passed 3 in 5 times.", right: "It passed three out of five times.", why: "proporção é out of", tag: gram.numbers-percentages }
  - { wrong: "Deploy tomorrow 9 am. (sem fuso, no Slack)", right: "Deploy tomorrow 9 am BRT.", why: "time distribuído: hora sem fuso é ambígua", tag: topic.async }
  - { wrong: "Hello everyone, hope you're doing well. As discussed…", right: "Quick recap: …", why: "no Slack, resumo na primeira linha", tag: vocab.slack }

dialogue:
  title: "Daily de quinta, 10:00 BRT. Release às 14h. Rodada completa."
  lines:
    - { speaker: Priya, text: "Morning all. Release day, so let's keep it tight. Let's go around — Ana first." }
    - { speaker: Ana, text: "Yesterday I finished the alerting rules for the new cluster. Today I'm on release support. One dependency: I'm shutting the old cluster down tomorrow, so I need checkout-worker off it by then. No blockers." }
    - { speaker: Priya, text: "Noted. Marcos?" }
    - { speaker: Marcos, text: "Base chart is merged as of Tuesday. Today I'm pairing with the data team on the new datasource. Nothing blocking me. Yuri, how did the rollout go with the chart?" }
    - { speaker: Yuri, text: "Good, thanks. Since Monday I've been on the checkout-worker migration. As of last night, the staging rollout is green. The one thing is the smoke test: it's flaky, three out of five runs, and I haven't figured out why yet.", note: "Semana + estado atual + o problema com número." }
    - { speaker: Marcos, text: "Is it the readiness probe? We had that on notifications-api." }
    - { speaker: Yuri, text: "Could be. Let's take that offline; I'll grab you after the call. The decision I need now: the plan was to deploy to prod before the 2 pm release, but given the flaky test, my recommendation is to deploy tomorrow at 9 am BRT instead. Priya, can I get a decision on that?", note: "Parking lot + recomendação + pedido de decisão." }
    - { speaker: Priya, text: "Ana, does tomorrow 9 am work with your shutdown?" }
    - { speaker: Ana, text: "Yes, if the deploy is done by noon I can shut the old cluster down in the afternoon." }
    - { speaker: Priya, text: "Then tomorrow 9 am it is. Yuri, post the recap in #platform. Anything for the parking lot?" }
    - { speaker: Yuri, text: "Two things: the smoke test root cause with Marcos, and who deletes the old Grafana dashboards. Not urgent." }
    - { speaker: Priya, text: "Good. Smoke test after the call, dashboards next week. That's the round. Thanks all." }
  notes:
    - '"Let''s keep it tight" = vamos ser breves.'
    - '"as of Tuesday" / "as of last night" marcam a data da informação.'
    - '"Then tomorrow 9 am it is" = fica combinado amanhã às 9.'
    - 'O parking lot tem dois itens, cada um com destino: depois da call e semana que vem.'

listening:
  lines:
    - { speaker: Priya, text: "Quick recap before we close. Prod deploy of checkout-worker moves to tomorrow at 9 am BRT, that's 1 pm for Marcos in Dublin. Ana shuts the old cluster down tomorrow afternoon, after Yuri confirms the deploy is done." }
    - { speaker: Marcos, text: "And I'll look into the flaky smoke test with Yuri after this call. My bet is the readiness probe, but I want to see the logs first. If it's the probe, it's a five-minute fix." }
    - { speaker: Ana, text: "One more thing for the parking lot: the old Grafana dashboards. Nobody owns them right now. I'd say we archive the folder next week rather than delete it, in case someone needs the history." }
    - { speaker: Priya, text: "Agreed, archive next week, Ana owns it. Yuri, please post the action items in #platform with the times in BRT and Dublin. That's it, thanks everyone." }
  questions:
    - { id: M01-05-l1, type: multiple_choice, prompt: "When is the prod deploy now?", options: ["Today before the release", "Tomorrow afternoon", "Next week", "Tomorrow at 9 am BRT"], answer: 3, explanation: '"moves to tomorrow at 9 am BRT"', tags: [comp.listening, topic.async] }
    - { id: M01-05-l2, type: fill_blank, prompt: "9 am BRT is ___ pm for Marcos in Dublin.", accepted: ["1", "one"], explanation: '"that''s 1 pm for Marcos in Dublin"', tags: [comp.listening, topic.async] }
    - { id: M01-05-l3, type: multiple_choice, prompt: "What does Ana wait for before shutting the old cluster down?", options: ["Marcos's logs", "Yuri confirming the deploy is done", "The release", "Priya's approval"], answer: 1, explanation: '"after Yuri confirms the deploy is done"', tags: [comp.listening] }
    - { id: M01-05-l4, type: multiple_choice, prompt: "What does Marcos think causes the flaky test?", options: ["The datasource", "The base chart", "The readiness probe", "The old cluster"], answer: 2, explanation: '"My bet is the readiness probe"', tags: [comp.listening] }
    - { id: M01-05-l5, type: multiple_choice, prompt: '"If it''s the probe, it''s a five-minute fix" means…', options: ["the fix is quick", "it will take five minutes to find", "the probe runs every five minutes", "he needs five more minutes"], answer: 0, explanation: "a five-minute fix = correção rápida.", tags: [vocab.standup] }
    - { id: M01-05-l6, type: multiple_choice, prompt: "What will happen to the old Grafana dashboards?", options: ["Deleted today", "Migrated tomorrow", "Nothing was decided", "Archived next week"], answer: 3, explanation: '"archive the folder next week rather than delete it"', tags: [comp.listening] }
    - { id: M01-05-l7, type: fill_blank, prompt: "Yuri should post the action items with the times in BRT and ___.", accepted: ["Dublin"], explanation: '"with the times in BRT and Dublin"', tags: [comp.listening, topic.async] }
    - { id: M01-05-l8, type: multiple_choice, prompt: "Who owns archiving the dashboards?", options: ["Yuri", "Marcos", "Ana", "Priya"], answer: 2, explanation: '"archive next week, Ana owns it"', tags: [comp.listening, vocab.standup] }

writing:
  prompt: |
    **Cenário:** o daily acabou. Ficou combinado: deploy do `checkout-worker` em produção amanhã às 9h BRT (13h em Dublin, onde está o Marcos); a Ana desliga o cluster antigo amanhã à tarde depois de você confirmar; o Marcos investiga o smoke test instável com você depois da call; os dashboards antigos do Grafana serão arquivados semana que vem pela Ana.

    **Tarefa:** escreva o follow-up no canal `#platform` (80–120 palavras): resumo na primeira linha, decisões, ações com dono e horário com fuso. Sem "hello", sem preâmbulo; detalhe vai para a thread.
  constraints:
    - { label: 'Começa com "Quick recap" ou "Recap"', pattern: "^\\s*(quick )?recap\\b" }
    - { label: '"Action items"', pattern: "\\baction items\\b" }
    - { label: "Horário com fuso (BRT e Dublin)", pattern: "\\bbrt\\b[\\s\\S]*\\bdublin\\b|\\bdublin\\b[\\s\\S]*\\bbrt\\b" }
    - { label: '"thread" para o detalhe', pattern: "\\bthread\\b" }
  rubric:
    - Resumo na primeira linha, sem preâmbulo
    - Decisões e ações com dono
    - Horário com fuso
    - Concisão e tom de Slack
  model: >-
    Quick recap from standup: the checkout-worker prod deploy moves to tomorrow at 9 am BRT (1 pm Dublin);
    the old cluster shutdown happens tomorrow afternoon, after I confirm the deploy is done. Action items:
    Yuri — prod deploy at 9 am BRT and confirmation in this channel; Marcos — look into the flaky smoke test
    with me right after the call; Ana — shut the old cluster down after my confirmation, and archive the old
    Grafana dashboards next week. Thread below for the smoke test details. Shout if I missed anything.
  minWords: 80
  maxWords: 120
  tags: [comp.writing, vocab.slack, topic.async]

speaking:
  modeA:
    prompt: Grave a sua rodada inteira em até 60 segundos, como se fosse quinta-feira do cenário: semana desde segunda, estado atual, o problema com número, sua recomendação, o pedido de decisão e o que vai para o parking lot.
    maxSeconds: 60
    targetPhrases:
      - since Monday I've been
      - as of last night
      - the plan is to
      - flaky
      - out of
      - I haven't figured out why yet
      - my recommendation is
      - given that
      - I'd rather
      - can I get a decision
      - let's take that offline
      - parking lot
      - does that work
      - no blockers
      - by
    checklist:
      - Usei present perfect com "since" para a semana e past simple para o fato fechado?
      - Dei o problema com número (three out of five)?
      - Recomendei antes de pedir a decisão?
      - Mandei o que não cabe para o parking lot com dono?
      - Fiquei abaixo de 60 segundos?

quiz:
  - { id: M01-05-q1, type: fill_blank, prompt: "Since Monday I ___ (be) on the migration.", accepted: ["have been", "'ve been"], explanation: "since + present perfect.", tags: [gram.since-for, br.since-present] }
  - { id: M01-05-q2, type: multiple_choice, prompt: "Which one asks for a decision without deciding alone?", options: ["I will not deploy today.", "Maybe we deploy, maybe not.", "My recommendation is tomorrow 9 am. Priya, can I get a decision on that?", "Whatever you want."], answer: 2, explanation: "Recomendação + pedido de decisão. As outras decidem sozinho ou não dizem nada.", tags: [topic.daily, vocab.standup] }
  - { id: M01-05-q3, type: error_correction, prompt: "Let's discuss about the smoke test after the call.", accepted: ["Let's discuss the smoke test after the call.", "Let's discuss the smoke test after the call", "Let's take the smoke test offline.", "Let's talk about the smoke test after the call."], explanation: "discuss é transitivo direto: discuss X. (talk about X também vale.)", tags: [br.discuss-about] }
  - { id: M01-05-q4, type: translate, prompt: "Passou em três de cinco execuções.", accepted: ["It passed three out of five runs.", "It passed three out of five runs", "It passed three out of five times.", "It passed three out of five times", "It passed 3 out of 5 runs.", "It passed 3 out of 5 runs"], explanation: "Proporção: X out of Y.", tags: [gram.numbers-percentages, vocab.standup] }
  - { id: M01-05-q5, type: multiple_choice, prompt: '"Let''s take that offline" means…', options: ["let's disconnect from the internet", "let's discuss it outside this meeting", "let's cancel it", "let's do it without the team"], answer: 1, explanation: "take it offline = tratar fora da reunião.", tags: [vocab.standup] }
  - { id: M01-05-q6, type: reorder, prompt: "Coloque na ordem certa.", tokens: ["work", "does", "that", "everyone", "for"], answer: "does that work for everyone", explanation: "Fechamento padrão de um combinado.", tags: [vocab.standup, gram.question-forms] }
  - { id: M01-05-q7, type: multiple_choice, prompt: "Which Slack follow-up is the best first line?", options: ["Quick recap: prod deploy tomorrow 9 am BRT (1 pm Dublin).", "Hello everyone, hope you are well.", "As discussed in our meeting today…", "deploy tomorrow"], answer: 0, explanation: "Resumo na primeira linha, com hora e fuso. Sem preâmbulo; sem hora é ambíguo.", tags: [vocab.slack, topic.async] }
  - { id: M01-05-q8, type: match, prompt: "Associe a etapa da decisão à frase.", pairs: [{ left: "contexto", right: "The smoke test is flaky, three out of five." }, { left: "recomendação", right: "My recommendation is to deploy tomorrow." }, { left: "motivo", right: "Given that the release is at 2 pm, I'd rather not rush it." }, { left: "pedido de decisão", right: "Can I get a decision on that?" }], explanation: "Contexto → recomendação → motivo → pedido de decisão → fechamento.", tags: [topic.daily] }

review:
  count: 5
  preferTags: [gram.since-for, gram.by-until, br.waiting-no-prep]

srsCards:
  - { front: "desde segunda estou na migração", back: "Since Monday I've been on the migration", tag: gram.since-for }
  - { front: "desde ontem à noite, o rollout está verde", back: "As of last night, the rollout is green", tag: vocab.standup }
  - { front: "o plano é fazer o deploy até o meio-dia", back: "The plan is to deploy by noon", tag: gram.by-until }
  - { front: "passou em três de cinco execuções", back: "It passed three out of five runs", tag: gram.numbers-percentages }
  - { front: "ainda não descobri por quê", back: "I haven't figured out why yet", tag: gram.present-perfect }
  - { front: "vamos tratar disso fora do daily", back: "Let's take that offline", tag: vocab.standup }
  - { front: "minha recomendação é…", back: "My recommendation is…", tag: vocab.standup }
  - { front: "considerando que o release é às 14h", back: "Given that the release is at 2 pm", tag: vocab.standup }
  - { front: "preciso de uma decisão sobre isso", back: "Can I get a decision on that?", tag: vocab.standup }
  - { front: "funciona para todo mundo?", back: "Does that work for everyone?", tag: vocab.standup }
  - { front: "resumindo (Slack)", back: "Quick recap:", tag: vocab.slack }
  - { front: "ações combinadas, com dono", back: "Action items:", tag: vocab.slack }

completion:
  quizMin: 0.75
  writingMin: 3
  speakingRequired: true
```

- [ ] **Step 3: Verifique**

Run: `pnpm content:build && pnpm test && pnpm typecheck`
Expected: `(5 aula(s) com conteúdo)`; PASS (223 + 6 = 229). A tag `gram.numbers-percentages` existe em `tags.yaml`. Atenção aos `meaning` que podem colidir com o glossário (`action item`, `parking lot`, `flaky`): os textos acima já diferem dos de `daily.yaml`; se `crossValidate` acusar, ajuste o `meaning` da aula.

- [ ] **Step 4: Commit**

```bash
git add content/modules/M01/lessons/M01-05.yaml tests/lessons-m01.test.ts
git commit -m "content(M01): aula M01-05 — standup completo com 3 personas e parking lot"
```

---

### Task 10: Avaliação do M01 com 15 itens cobrindo as cinco aulas

**Files:**
- Modify: `content/modules/M01/assessment.yaml` (itens A11–A15 e rebalanceamento), `tests/assessment-content.test.ts` (novo)

**Interfaces:**
- Consumes: aulas M01-01…05 (T6–T9), tag `gram.time-expressions` (T5), `ModuleAssessmentSchema` (T1), rotas (T3).
- Produces: avaliação do M01 completa (15 itens, todas as tags `gram.*`/`br.*` das 5 aulas representadas), com teste de conteúdo.

- [ ] **Step 1: Teste que falha** — crie `tests/assessment-content.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { loadContent, crossValidate } from "../shared/content-loader.ts";
import { ruleBasedFeedback } from "../server/writing-feedback.ts";
import { wordOverlap } from "../shared/speech-compare.ts";

const bundle = loadContent("content");
const a = bundle.moduleAssessments["M01"]!;
const m01 = Object.values(bundle.lessons).filter((l) => l.module === "M01");

describe("content/modules/M01/assessment.yaml", () => {
  it("has 15 items mixing at least 4 types, with balanced multiple-choice keys", () => {
    expect(a.items).toHaveLength(15);
    expect(new Set(a.items.map((q) => q.type)).size).toBeGreaterThanOrEqual(4);
    const mc = a.items.filter((q) => q.type === "multiple_choice");
    const counts = new Map<number, number>();
    for (const q of mc) if (q.type === "multiple_choice") counts.set(q.answer, (counts.get(q.answer) ?? 0) + 1);
    expect(Math.max(...counts.values())).toBeLessThanOrEqual(Math.floor(mc.length * 0.5));
    expect(crossValidate(bundle)).toEqual([]);
  });
  it("covers every gram.* and br.* tag taught in the five M01 lessons", () => {
    const taught = new Set(m01.flatMap((l) => l.tags).filter((t) => t.startsWith("gram.") || t.startsWith("br.")));
    const assessed = new Set(a.items.flatMap((q) => q.tags));
    const missing = [...taught].filter((t) => !assessed.has(t));
    expect(missing, `tags do módulo sem item na avaliação: ${missing.join(", ")}`).toEqual([]);
  });
  it("writing model passes its own constraints and the BR detector; target phrases are clean", () => {
    const fb = ruleBasedFeedback(a.writing.model, a.writing, bundle.brErrors);
    expect(fb.withinLength).toBe(true);
    expect(fb.constraints.filter((c) => c.met !== null).every((c) => c.met)).toBe(true);
    expect(fb.findings).toEqual([]);
    for (const p of a.speaking.targetPhrases) expect(wordOverlap(p, p)).toBe(1);
  });
});
```

- [ ] **Step 2: Rode para ver falhar**

Run: `pnpm test tests/assessment-content.test.ts`
Expected: FAIL — 10 itens, não 15; tags faltando.

- [ ] **Step 3: Substitua a lista `items:` inteira de `content/modules/M01/assessment.yaml`** pelos 15 itens abaixo (A01–A04, A06, A07, A09 mantidos; A05, A08, A10 reescritos; A11–A15 novos; múltiplas escolhas balanceadas: índices 0:1, 1:2, 2:2, 3:1). Troque também o comentário de cabeçalho por `# Avaliação do módulo M01: 15 itens cobrindo as cinco aulas + escrita + fala.`

```yaml
items:
  - { id: M01-A01, type: fill_blank, prompt: "I ___ (work) on the alerting rules since Tuesday.", accepted: ["have been working", "'ve been working"], explanation: "since + atividade que continua: present perfect continuous.", tags: [gram.since-for, br.since-present] }
  - { id: M01-A02, type: multiple_choice, prompt: "Which sentence sets a deadline?", options: ["I'll be on this until Friday.", "I'll have it done by Friday.", "I worked on it for Friday.", "I'm working on it since Friday."], answer: 1, explanation: "by = prazo final. until = duração; for/since marcam tempo decorrido.", tags: [gram.by-until, br.until-by] }
  - { id: M01-A03, type: error_correction, prompt: "I didn't merge the PR yet.", accepted: ["I haven't merged the PR yet.", "I haven't merged the PR yet"], explanation: "yet em negativa pede present perfect.", tags: [gram.still-yet-already, gram.present-perfect] }
  - { id: M01-A04, type: multiple_choice, prompt: "Which update is the clearest?", options: ["It's almost done, almost.", "Everything is broken, I don't know.", "The tests are taking longer than expected; I'll need another day.", "Sorry, sorry, I'm so slow."], answer: 2, explanation: "Diz o problema, o impacto e o novo prazo. As outras não dão informação.", tags: [topic.daily, vocab.standup] }
  - { id: M01-A05, type: fill_blank, prompt: "The rollout test depends ___ the base chart from Marcos, so I'm still waiting on it.", accepted: ["on"], explanation: "depend on; e wait on/for. As duas com on.", tags: [br.depend-of, br.waiting-no-prep] }
  - { id: M01-A06, type: error_correction, prompt: "I have a doubt about the rollout.", accepted: ["I have a question about the rollout.", "I have a question about the rollout", "I'm not sure about the rollout.", "I'm not sure about the rollout"], explanation: "doubt = desconfiança; pergunta é question.", tags: [br.doubt] }
  - { id: M01-A07, type: translate, prompt: "O job está dando erro de permissão.", accepted: ["The job is failing with a permission error.", "The job is failing with a permission error", "The job is throwing a permission error.", "The job is throwing a permission error", "The job fails with a permission error.", "The job fails with a permission error"], explanation: "\"Giving error\" é calque. Use fail with / throw / time out.", tags: [br.giving-error, vocab.errors] }
  - { id: M01-A08, type: translate, prompt: "Passou em três de cinco execuções.", accepted: ["It passed three out of five runs.", "It passed three out of five runs", "It passed three out of five times.", "It passed three out of five times", "It passed 3 out of 5 runs.", "It passed 3 out of 5 runs"], explanation: "Proporção: X out of Y.", tags: [gram.numbers-percentages, vocab.standup] }
  - { id: M01-A09, type: multiple_choice, prompt: "Yesterday I ___ the build stage.", options: ["fix", "have fixed", "fixed", "am fixing"], answer: 2, explanation: "yesterday fecha o momento: past simple.", tags: [gram.present-perfect] }
  - { id: M01-A10, type: error_correction, prompt: "I pretend to discuss about the deploy with Ana, who is in the new cluster.", accepted: ["I plan to discuss the deploy with Ana, who is on the new cluster.", "I plan to discuss the deploy with Ana, who is on the new cluster", "I intend to discuss the deploy with Ana, who is on the new cluster.", "I intend to discuss the deploy with Ana, who is on the new cluster"], explanation: "pretend = fingir (plan/intend); discuss é transitivo direto; cluster pede on.", tags: [br.pretend, br.discuss-about, gram.prepositions-infra] }
  - { id: M01-A11, type: multiple_choice, prompt: "Today I ___ shadow Marcos on the deploy.", options: ["going to", "am going to", "will going", "go to"], answer: 1, explanation: "Plano decidido: be going to (com o verbo be).", tags: [gram.future-plans] }
  - { id: M01-A12, type: error_correction, prompt: "Actually I'm working at the platform team now. (= atualmente)", accepted: ["Currently I'm working at the platform team.", "Currently I'm working at the platform team now.", "I'm currently working at the platform team.", "Right now I'm working at the platform team.", "At the moment I'm working at the platform team."], explanation: "actually = na verdade. Atualmente é currently / right now / at the moment.", tags: [br.actually] }
  - { id: M01-A13, type: multiple_choice, prompt: "I can move the cutover ___ two days of the dashboards landing.", options: ["until", "by", "for", "within"], answer: 3, explanation: "Janela relativa = within. by é prazo; until, duração.", tags: [gram.time-expressions, gram.by-until] }
  - { id: M01-A14, type: error_correction, prompt: "I stayed until late and we lost the deadline anyway.", accepted: ["I stayed late and we missed the deadline anyway.", "I stayed late and we missed the deadline anyway", "I stayed late but we missed the deadline anyway.", "I worked late and we missed the deadline anyway."], explanation: '"until late" não existe; prazo se perde com miss.', tags: [br.until-late, br.lose-the-deadline] }
  - { id: M01-A15, type: multiple_choice, prompt: "Could you ___ how the secret gets injected?", options: ["walk me through", "explain me", "explain to me about", "explaining me"], answer: 0, explanation: "walk me through / explain X to me. \"explain me\" é calque.", tags: [br.explain-me, gram.modals-polite] }
```

Regra: nunca marque uma tag que o item não testa. Se o teste de cobertura listar alguma tag faltante, crie um item que a teste de verdade e remova outro para manter 15.

- [ ] **Step 4: Verifique**

Run: `pnpm content:build && pnpm test && pnpm typecheck`
Expected: PASS (229 + 3 = 232), incluindo `tests/assessment-routes.test.ts` (que agora conclui as 5 aulas antes do fluxo completo). Se o teste de cobertura de tags listar alguma faltante, acrescente-a a um item que de fato a teste ou crie o item e remova outro para manter 15; nunca marque tag que o item não testa.

- [ ] **Step 5: Commit**

```bash
git add content/modules/M01/assessment.yaml tests/assessment-content.test.ts
git commit -m "content(M01): avaliação do módulo com 15 itens cobrindo as cinco aulas"
```

---

### Task 11: Documentação e verificação no navegador

**Files:**
- Modify: `README.md`, `AGENTS.md`

Executada pelo controlador (subagentes não têm navegador). Defeito encontrado vira correção despachada, não patch direto.

- [ ] **Step 1: Documentação**

`AGENTS.md`: estado → `Estado atual: etapas E0–E3 entregues e E5 em andamento (M01 completo: 5 aulas + avaliação do módulo). A E4 (Claude API) está fora do roteiro por decisão do usuário (sem créditos de API): tudo segue por regras e heurísticas locais, sem LLM. Próximas: E5 (M02–M06 + avaliação de nível), E6 adaptação.`; em "Estrutura", `content/` ganha `modules/Mxx/assessment.yaml (avaliação do módulo)` e `server/` ganha `assessment.ts (avaliação de módulo)`. Em "Conteúdo", acrescente: `- Avaliação de módulo: 15 itens que cobrem todas as tags gram.*/br.* das aulas do módulo (teste de guarda), gabarito balanceado, escrita + fala com os mesmos motores das aulas. Aprovação: itens ≥ 75 %, escrita ≥ 3, fala ≥ 3.`

`README.md`: na linha "Páginas:", acrescente `` `/modules/:id/assessment` avaliação do módulo (liberada quando todas as aulas do módulo estão concluídas) ``; na seção "Estado atual", atualize para E0–E3 + M01 completo, sem E4.

- [ ] **Step 2: Verificação no Chrome** (`make dev`; banco real; `make backup-db` antes, porque a migração 2 reconstrói `attempts`)

1. `/modules/M01`: 5 aulas disponíveis; card da avaliação bloqueado com "Conclua as 5 aulas (1 feitas)" (a M01-02 já estava concluída no banco real).
2. Abrir M01-01, M01-03, M01-04, M01-05: cada uma renderiza os 13 blocos; escuta toca com vozes diferentes; quiz de 8 com os tipos `match` e `reorder` funcionando; escrita mostra restrições; conclusão insere os cards.
3. Concluir as 4 aulas (quiz ≥ 75 %, escrita com nota, uma fala). `/modules/M01`: card "Todas as aulas concluídas" → "Fazer avaliação".
4. `/modules/M01/assessment`: intro → 15 itens sem feedback → escrita (nota obrigatória) → fala (obrigatória) → resultado com aprovado/reprovado, regra, tags fracas, revisão item a item. Recarregar no meio retoma.
5. `/trilha`: badge "aprovado" no M01 (se aprovado). `/` linha do tempo mostra a avaliação. `/review`: cards das aulas novas na fila.
6. `pnpm content:build && pnpm typecheck && pnpm test && pnpm build` verdes.

- [ ] **Step 3: Commit**

```bash
git add README.md AGENTS.md
git commit -m "docs: avaliação de módulo e M01 completo; estado da E5"
```

Relate ao final: contagem de testes, o que foi verificado no navegador, decisões que desviaram do plano, e a lista de conteúdo para revisão humana (4 aulas + avaliação).
