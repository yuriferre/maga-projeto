# M18 — Observabilidade, SLOs e performance

Spec (trilha, nível 3): VOC+SPK; gram.describing-trends (já existe);
pré-requisito M12; M18-05 é `simulation` (apresentar análise de
degradação com gráfico). Pass padrão.

## Tarefas

- **T1** — `module.yaml` + esqueleto `tests/lessons-m18.test.ts` +
  branch `feat/e5-m18`.
- **T2** — 4 padrões BR de métricas + tags + guardas:
  - `the latency raised` → rose/went up (raise é transitivo;
    sujeitos = substantivos de métrica)
  - `the CPU is in 80%` → at 80% ("em 80%" é calque)
  - `we have a latency of 200ms` → "latency is 200ms" ("temos uma
    latência de")
  - `it overpassed the limit` → exceeded (verbo inexistente)
  - + tag `vocab.slo` (SLO, error budget, burn rate)
- **T3–T7** — 5 aulas:
  - M18-01 metrics, logs, traces (emit, scrape, sample, correlate)
  - M18-02 descrevendo gráficos (spiked, dropped, plateaued,
    "p99 went from… to…")
  - M18-03 SLOs e error budget ("we're burning budget", "within
    SLO", "burn rate", "the SLI is…")
  - M18-04 performance (bottleneck, throughput, saturation,
    "CPU-bound", "hitting connection limits")
  - M18-05 [SIM] apresentar análise de degradação com gráfico
- **T8** — avaliação: 15 itens cobrindo as tags + variantes.
- **T9** — validação via API na cópia, AGENTS.md, resultado no plano.

## Desvios previstos

- Padrões com listas fechadas de sujeitos (política conservadora).

## Resultado

- **Estado:** completo na `feat/e5-m18` — 8 commits.
- **Aulas:** 5 (M18-01..05) — três pilares/verbos de telemetria →
  descrever gráficos → SLOs e error budget → performance/gargalos →
  simulação de análise de degradação.
- **Avaliação:** 15 itens cobrindo as 8 tags ensinadas
  (vocab.observability, vocab.slo, gram.describing-trends,
  br.metric-raised, br.have-a-latency, br.in-percent, br.overpassed,
  br.of-weekday); 6 testes de variante.
- **Validação:** content:validate ok (90 aulas, 149 tags,
  85 padrões) · 1055 testes · typecheck e build limpos.
- **API na cópia:** elegível 5/5 → finish prematuro 409 →
  15/15 itens → ref inválida 400 → escrita 4 (0 findings) →
  fala 4,5 (58 s) → aprovado → segundo finish 409.

## Desvios registrados

- `brErrors` das aulas carregam tags além das cobradas pela guarda
  (que conta só l.tags + quiz + listening): discuss-about, doubt,
  it-not-works, more-er, third-person-s, in-weekday, i-wait-reply —
  cobertura extra, não exigida.
- Duas questões de fill_blank com duas lacunas foram reescritas —
  o schema exige exatamente um `___`.
- Gabarito rebalanceado em M18-03/04/05 (o limite conta MC de
  listening + quiz juntos).
- A escrita da avaliação exige `selfScore` no corpo para a nota
  registrar (mesma descoberta de M11).
