# M19 — Segurança

Spec (trilha, nível 3): VOC+WRI+SPK; pré-requisito M15;
M19-05 é `simulation` (discutir finding de pentest e negociar
prazo). Pass padrão.

## Tarefas

- **T1** — `module.yaml` + esqueleto `tests/lessons-m19.test.ts` +
  branch `feat/e5-m19`.
- **T2** — 4 padrões BR de segurança + guardas:
  - `the attacker explored the vuln` → exploited ("explorou" ≠
    explored; objeto restrito a vuln/flaw/weakness)
  - `the finding is critic` → critical (só com verbos de estado —
    "a film critic" não dispara)
  - `in compliance to` → with
  - `must to fix` → must fix (modais, sem "to" — "want/have/used
    to" ficam fora)
- **T3–T7** — 5 aulas:
  - M19-01 vocabulário (vulnerability, CVE, exposure, least
    privilege, hardening, secret sprawl, lateral movement)
  - M19-02 reportando (urgência sem pânico, canal certo, o que
    NÃO escrever em canal aberto)
  - M19-03 riscos e compliance ("this violates…", "we're required
    to…", audit, evidence, compensating control)
  - M19-04 respondendo a findings (remediação, aceitação de risco,
    prazo, "we'll track it under…")
  - M19-05 [SIM] discutir um finding de pentest e negociar prazo
- **T8** — avaliação: 15 itens + variantes.
- **T9** — validação via API na cópia, AGENTS.md, resultado no plano.

## Desvios previstos

- Padrões com listas fechadas de sujeitos/objetos (conservador).

## Resultado

- **Estado:** completo na `feat/e5-m19` — 8 commits.
- **Aulas:** 5 (M19-01..05) — vocabulário de segurança →
  reporte com urgência sem pânico → riscos/compliance →
  respondendo a findings → simulação de finding de pentest.
- **Avaliação:** 15 itens cobrindo as 6 tags ensinadas
  (vocab.security, br.explored-vuln, br.is-critic,
  br.in-compliance-to, br.modal-to, br.until-by); 5 testes de
  variante.
- **Validação:** content:validate ok (95 aulas, 153 tags,
  89 padrões) · 1099 testes · typecheck e build limpos.
- **API na cópia:** elegível 5/5 → finish prematuro 409 →
  15/15 itens → ref inválida 400 → escrita 4 (0 findings) →
  fala 4,5 (55 s) → aprovado → segundo finish 409.

## Desvios registrados

- `modal-to` exclui `can` de propósito: "a can to open" é
  legítimo (can = lata) — a lista fica com must/should/could/
  would/may/might/shall.
- `explored-vuln` restringe o objeto a vuln/flaw/weakness/cve/bug
  — "we explored the options" fica fora.
- Gabarito rebalanceado em M19-02/03/05 (MC de listening + quiz
  contam juntos).
- Um `\'` em aspas duplas quebrou o YAML — apóstrofo dentro de
  `"…"` não leva escape.
