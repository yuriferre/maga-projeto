# Plano — E5 · M22 · Riscos e trade-offs (Nível 4)

## Spec (docs/planejamento/02-trilha.md)

- **Objetivo:** apresentar riscos, trade-offs e mitigações de
  forma que ajude a decidir.
- **Competências:** SPK, WRI; `gram.probability-modals`.
- **Pré-requisitos:** M21.
- **5 aulas:**
  1. Linguagem de risco: likelihood, impact, "there's a
     chance that", "worst case", "the exposure here is…"
  2. Trade-offs: "we're trading X for Y", "the downside
     is…", "on balance…"
  3. Mitigações e planos B: "to reduce that risk…", "if
     that happens, we…"
  4. Para decisão: recomendação clara + alternativas + o que
     você precisa de quem decide
  5. Prática: análise de risco escrita de uma migração de
     banco

## Padrões BR novos (T2 — testes primeiro)

| id | erro | certo | guarda |
|---|---|---|---|
| in-risk | we are in risk | we're at risk | "works in risk management" |
| risk-to | the risk to fail | risk of failing | "risk to reward", "chance to win" (substantivo certo) |
| compensate-risk | the discount compensates the risk | offsets the risk | "compensate the victims" |
| is-danger | the change is danger | dangerous | "the danger is real", "a danger zone" |

Tags novas: `gram.probability-modals`, `vocab.risk`,
`br.in-risk`, `br.risk-to`, `br.compensate-risk`,
`br.is-danger`.

## Tarefas

- [ ] T1: branch `feat/e5-m22` + `module.yaml` + esqueleto
- [ ] T2: 4 padrões BR + tags + guardas (RED → GREEN)
- [ ] T3: M22-01 — linguagem de risco (likelihood × impact)
- [ ] T4: M22-02 — trade-offs ("we're trading X for Y")
- [ ] T5: M22-03 — mitigações e plano B
- [ ] T6: M22-04 — apresentar para decisão (recomendação + ask)
- [ ] T7: M22-05 — análise de risco escrita da migração
- [ ] T8: avaliação do módulo + variantes
- [ ] T9: validação API + `AGENTS.md` + fechamento

## Resultado

M22 completo na `feat/e5-m22` — 9 commits:

- `module.yaml` + plano + esqueleto de testes
- 4 padrões BR novos (`in-risk`, `risk-to`,
  `compensate-risk`, `is-danger`) + tags
  `gram.probability-modals` e `vocab.risk`
- 5 aulas: matriz likelihood × impact → trade-offs
  ("we're trading X for Y", "on balance") → mitigações
  (preventiva + plano B + gatilho + dono, "should the lag
  grow") → para decisão (recomendação → alternativas →
  residual → ask, "walk-away condition") → prática:
  análise de risco escrita da migração (risk register,
  assumption log, watch item, go/no-go)
- Avaliação: 15 itens cobrindo as 10 tags `gram/br/vocab`
  do módulo; 6 testes de variante; escrita = análise de
  risco; fala de 60 s

Validação: `content:validate` ok (110 aulas, 166 tags, 100
padrões) · 1242/1242 testes · typecheck e build limpos.
API na cópia: elegível 5/5 → `finish` prematuro 409 → ref
inválida 400 → 15/15 itens → escrita 4 (0 findings) → fala
5 → **aprovado** (`module|M22`) → segundo `finish` 409.

## Desvios registrados

- Mini-markdown não aceita negrito dentro de itálico — três
  trechos do grammar de M22-03 perderam o `**` interno.
- O balanceamento conta MC de listening + quiz juntos
  (máx 4 na aula, `floor(n/2)` na avaliação) — rebalanceei
  M22-02 (6×), M22-03 (8×), M22-04 (5×), M22-05 (6×) e o
  A09 da avaliação (3× com 5 MC).
- Modelo de escrita do M22-05 nasceu com 98 palavras para
  mínimo 100 — expandido.
- `risk-to` ficou restrito a risk/possibility/probability/
  likelihood + verbos fechados — "chance to win" e
  "risk to reward" são legítimos.
- `is-danger` exclui "danger zone/is/of/that/when" no
  lookahead — "the danger is real" não dispara.
