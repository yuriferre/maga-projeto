# M32 — Onboarding em empresa internacional

Spec (`content/levels.yaml`): SPK/WRI/CNF, pré-req M26, 4 aulas,
avaliação "Rubrica dos cenários de primeira semana". Último módulo
do Nível 5 — depois dele vem a avaliação de nível.

## Tarefas

- T1: `module.yaml` + `tests/lessons-m32.test.ts` (esqueleto)
- T2: 4 padrões BR novos com guardas:
  - `new in the company` → new to the team/company
    ("new in town" idiomático, fora)
  - `in my first day` → on my first day
    (só singular — "in the first days" fora)
  - `I need a help` → I need help
    ("a help desk" fora)
  - `the responsible for X` → the owner / who owns
    ("the responsible person", "held responsible for" fora)
  - mais a tag `vocab.onboarding`
- T3: M32-01 — primeiras semanas (se apresentar, mensagem de intro,
  perguntas sem medo)
- T4: M32-02 — construindo relação (1:1s de conhecimento, coffee chat)
- T5: M32-03 — pedindo contexto e documentação ("is there a doc
  for…", "who owns…")
- T6: M32-04 — [SIM] primeira semana em 3 cenários curtos
- T7: avaliação 15 itens + testes de variante
- T8: docs, validação API e fechamento

## Resultado

M32 completo na `feat/e5-m32`:

- `module.yaml` com aprovação padrão (itens ≥ 75 %, escrita ≥ 3, fala ≥ 3)
- 4 padrões BR novos com guardas: `new-in-company`, `in-first-day`,
  `a-help`, `the-responsible` (+ tag `vocab.onboarding`)
- 4 aulas: primeira semana (intro de 30 s, mensagem no canal, pergunta
  sem medo) → rede (1:1s de conhecimento, coffee chat "no agenda",
  mapa de posse) → contexto e docs (pesquisa declarada, "who owns",
  "the story behind", a lacuna vira contribuição) → [SIM] primeira
  semana em 3 cenários (segunda/quarta/sexta)
- Avaliação: 15 itens cobrindo as 11 tags (10 `br.*` +
  `vocab.onboarding`); 5 testes de variante; escrita = recap de sexta
  + autoavaliação; fala = semana comprimida de 120 s
- Validação: 1680 testes, typecheck e build limpos; API na cópia:
  elegível 4/4 → finish prematuro 409 → ref inválida 400 → 15/15 →
  escrita 4 (0 findings) → fala 4,5 → aprovado (`module|M32`) →
  duplicado 409

## Desvios registrados

- Tags `br.doubt-question`/`br.assist-meeting` são os IDs dos padrões;
  as tags reais são `br.doubt`/`br.assist` — corrigido após o
  cross-validation flagrar.
- Reorder do M32-04 tinha "a" e "deploy" sobrando nos tokens —
  removidos para casar com a resposta.
- Modelo da avaliação ficou com 96 palavras (mín 100) — expandido.
